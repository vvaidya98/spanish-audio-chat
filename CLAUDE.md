# CLAUDE.md — Conversation Amigo

*This file is read automatically by Claude Code at the start of every session. It contains everything needed to work on this project without re-explanation. Do not modify it during a session unless explicitly instructed.*

---

## Project Overview

Conversation Amigo (renamed from "Spanish Audio Chat" in v1.0m — the repo folder, GitHub repo, Netlify site, Railway service, and IndexedDB database name are all unchanged and still literally "spanish-audio-chat"; only user-facing brand text changed) is a beginner-friendly conversational Spanish practice app with audio voice chat powered by Claude. Users pick a conversation scenario, Claude initiates in Spanish, the user speaks via Web Speech API, Claude responds in Spanish, and provides feedback (corrections + encouragement) in English. The app emphasizes **comprehension + comprehensibility over grammar perfection** — it's designed to help users understand native Spanish speakers and be understood by them, with grammar polish coming later as confidence builds.

**Live URL:** https://spanish-audio-chat.netlify.app  
**Backend URL:** https://spanish-audio-chat-production.up.railway.app  
**GitHub:** https://github.com/vvaidya98/spanish-audio-chat (public)  
**Current Version:** v1.2a (Live in Production) — Prompt #035 (SAC-079: Grammar Breakdown Icons — a new ⓘ icon in both the Display Spanish block and the Transcript, next to each sentence, showing a short "how this differs from English" note (phrase, literal translation, natural English syntax, a one-line pattern observation) generated by Claude in the background right after a story loads/regenerates, never blocking Play. This round's own initial pitch (Prompt #034, before it was renumbered #035 for the same-day numbering collision with the SAC-078 refinement round) described a "hard-coded, static" 18-example data structure — flagged before building that this couldn't actually work, since every story in this app is dynamically generated per scenario/difficulty/regenerate/custom-topic, so 18 fixed sentences would almost never match real content; Vinay chose real per-story generation via a new `/api/generate-sentence-explanations` endpoint instead. New `ExplanationIcon.jsx` (two presentational exports, `ExplanationIcon`/`ExplanationPanel`, deliberately not one self-contained unit — the panel needs to render as a block-level sibling below the icon row, matching this file's existing `openTranslationIdx`/🌐 pattern, not squeezed inside it). See Key Architecture Decisions and Decisions Log below). v1.1f shipped Prompt #034 (SAC-078 refinement: swapped the pulse's `scale(1.5)` bounce for a gentler opacity/color breathing fade (1.2s per pulse × 3 = 3.6s total, up from 0.6s × 3 = 1.8s), same 3-iteration cap. The prompt's own CSS snippet referenced a keyframe name (`play-pulse`) and CSS variables (`--text-primary`/`--text-muted`) that don't exist in this codebase — kept the real `playButtonPulse` name and swapped in this project's actual tokens, using `--color-primary-light` (not the given `--text-muted`, a body-text-on-white token that would've looked mismatched against this button's solid teal circle + white icon styling) as the color's low point. Also updated the JS-side safety-net timeout from 1800ms to 3600ms to stay in sync with the new CSS duration — a mismatch there would have desynced the redundant JS stop from the still-running CSS animation. See Key Architecture Decisions and Decisions Log below). v1.1e shipped Prompt #033 (SAC-078: Limit Play Button Pulse to 3 Iterations — the pulse's CSS animation was `infinite`, so it kept animating indefinitely on any story a user never actually clicked Play on. Capped to exactly 3 iterations (0.6s each, 1.8s total, `forwards` fill-mode) at the CSS level — the actual stop mechanism, not just a JS timer — plus a new `pulseAnimationActive` state (separate from the sessionStorage-backed play-history tracking) that gets toggled off then back on via `requestAnimationFrame` so the CSS animation genuinely restarts on a second trigger within the same mount, not just the first. Also reverses a prior v1.0u decision to suppress the pulse after Regenerate — this round's prompt explicitly asked for a fresh 3x pulse on every new story, pre-built or custom, including regenerates. See Key Architecture Decisions and Decisions Log below). v1.1d shipped Prompt #032 (SAC-076: Difficulty Selector on Regenerate — `/api/generate-story` (pre-built scenarios) never actually had a difficulty concept before this round, despite the prompt's claim that it did "from SAC-071" — SAC-071 only ever added difficulty to the separate `/api/generate-custom-story` endpoint. Added a shared `DIFFICULTY_LEVELS`/`DIFFICULTY_GUIDE` to `server.js`, changed `story_cache`'s primary key from `scenario` alone to `(scenario, difficulty)`, made the in-memory `questionsCache` difficulty-aware too (an unrequested but necessary fix — without it, switching difficulty for the same scenario could show comprehension questions written for a different difficulty's story text), and extended `RegenerateModal.jsx` with a difficulty picker so both pre-built and custom regenerates can target Beginner/Intermediate/Advanced from one combined confirm step. See Key Architecture Decisions and Decisions Log below). v1.1c shipped Prompt #031 (SAC-077: UI/UX polish pass — header/version stacked, redundant "Change Mode" link removed, story header simplified + enlarged + truncating, Quick Translate de-emphasized, footer Speed/Clarity/Regenerate consolidated onto one larger/darker row. Also investigated and fixed the round's one open question ("vocabulary hover coverage: full or vocab-only?") rather than guessing — found that Custom Listening Topics (SAC-071) request only 8-12 vocabulary words while pre-built scenarios request every distinct word, an inconsistency introduced when SAC-071 was built, not a regression in older code. Fixed by matching pre-built's full-coverage prompt for custom stories too. See Key Architecture Decisions and Decisions Log below). v1.1b shipped Prompt #030 (SAC-075: fixed a real audio-leak race — navigating away from `ListeningStoryView` within ~175ms of triggering any speak() call (Play, Next/Prev, a speed change, a transcript sentence icon) could leave a scheduled utterance to fire after unmount, playing audio nothing could stop. The prompt's own two guessed root causes (cleanup not firing; `cancel()` not working) were both verified false before touching any code; see Key Architecture Decisions and Decisions Log below for the real cause and fix. Version bumped to v1.1b, not the prompt's suggested "v1.0a" — same collision this project already corrected once in the SAC-071 round). v1.1a shipped Prompt #029 (SAC-071: Custom Listening Topics with a Difficulty selector — users enter any topic, pick Beginner/Intermediate/Advanced, and get a generated story that plays through the same `ListeningStoryView.jsx` as pre-built scenarios, all v1.0z features included. Version bumped to v1.1a rather than the prompt's own suggested "v1.0a" — v1.0a was this project's actual first-ever released version, so reusing it now would create a real, confusing duplicate in the version history; v1.1a is the correct next value in the documented `v[major].[minor][letter]` format. See Key Architecture Decisions and Decisions Log below for this and the round's other corrections). v1.0z shipped Prompt #028 (SAC-073: persistent, SQLite-backed story cache + a non-blocking startup warmup that pre-generates all 8 scenarios shortly after boot, so a fresh deploy is fully warm without needing real user traffic to trigger it — see Key Architecture Decisions and Decisions Log below; the prompt's own literal spec called for `better-sqlite3` and an `await`-before-`listen()` blocking warmup, both corrected before building — the former contradicts this project's own documented reason for using `node:sqlite` instead, the latter would have delayed every deploy's port-binding by minutes, risking the same class of incident as the SAC-062 Node-18 crash-loop). v1.0y shipped Prompt #027 (SAC-074: decoupled the loading spinner from the Comprehension Check background fetch, cutting first-load time roughly in half; also surfaced and fixed a separate, major bug found while verifying it — every production deploy up to that point had been shipping React's *development* build, not production, due to a missing `NODE_ENV=production` in the build pipeline — see Key Architecture Decisions and Decisions Log below for both). v1.0x shipped Prompts #025 (mega build, SAC-052–064) + #026 (SAC-065–067) + a same-day quick fix (Spanish sentence numbers) + a later same-version round (SAC-069 Adjustable Clarity Mode, SAC-070 voice preference tuning, and a SAC-048 hardening fix for the Play button pulse) — #025 added: Listening UI refinements (Clarity Mode, x0.4 speed tier with x0.6 default, circular player buttons with a 2x Play button, word-click tooltips + a 🔊 replay icon on Display Spanish, Speed/Clarity relocated to a subtle bottom row), a Translation hybrid (`QuickTranslateModal.jsx` in-story overlay + a Back button on the standalone Translation page), and Settings/API tracking (clickable version badge → `AboutModal.jsx`, backed by a new SQLite-logged `/api/usage-stats` endpoint using Node's built-in `node:sqlite` — no new dependency). #026 added a confirmation modal (`RegenerateModal.jsx`) in front of Regenerate Story, skipped the old scenario-card confirm screen for Listening Mode (straight to loading), and added sentence-number prefixes to both Display Spanish and Display English. The later same-day-labeled v1.0x round (built across two parallel Claude Code sessions per Vinay's split-panel workflow) upgraded Clarity Mode from a boolean toggle to a 5-level dropdown (Off/Low/Medium/High/Ultra, SAC-069), reordered TTS voice preference to try Colombian/Latin American Spanish variants before Spain Spanish (SAC-070), and hardened the Play button's first-load pulse to survive a hard page reload via `sessionStorage` (SAC-048 follow-up). Prior version history: v1.0m → v1.0o (#015) → v1.0p (#016) → v1.0q (#017, shipped) → v1.0r (#018-#019) → v1.0s (#020, shipped) → v1.0t (#021 + corrections, shipped) → v1.0u (#022 + follow-up, shipped) → v1.0v + v1.0w (#023 + #024, shipped together) → v1.0x (#025 + #026 + quick fix + SAC-069/070/048-hardening, all shipped together) → v1.0y (#027, SAC-074 + the React-dev-bundle production fix) → v1.0z (#028, SAC-073) → v1.1a (#029, SAC-071) → v1.1b (#030, SAC-075) → v1.1c (#031, SAC-077) → v1.1d (#032, SAC-076) → v1.1e (#033, SAC-078) → v1.1f (#034, SAC-078 refinement) → v1.2a (#035, SAC-079).  
**Stack:** React 18 + Vite (frontend, Netlify) | Node.js + Express (backend proxy, Railway)

---

## Repository / Project Structure

```
spanish-audio-chat/
├── CLAUDE.md              ← this file
├── PENDING.md             ← open decisions + backlog (SAC- prefix)
├── README.md
├── .env                   ← local secrets (gitignored)
├── .env.example           ← template with key names, no values
├── .gitignore             ← must exclude .env, node_modules/, dist/
├── index.html             ← Vite entry point (at root, not in public/)
├── package.json
├── server.js              ← Express backend
├── data/                  ← SQLite (gitignored): API usage log + persistent story cache (see Tech Stack)
├── vite.config.js
├── tailwind.config.js
├── postcss.config.js
├── Procfile               ← Railway deployment
├── railway.json           ← Railway build/start override (backend-only, skips frontend build)
├── LICENSE                ← MIT
├── public/                ← static assets (if needed)
└── src/
    ├── main.jsx           ← React entry
    ├── App.jsx            ← root component
    ├── index.css          ← Tailwind imports
    ├── db.js              ← IndexedDB session storage (local-only, no backend)
    ├── api.js             ← fetch wrapper, VITE_API_URL-aware (dev proxy vs. prod Railway URL)
    ├── analytics.js       ← logEvent(), console-only for now
    ├── speechUtils.js     ← applySpanishVoice() (voice selection, SAC-070) + SPEAK_START_DELAY_MS
    └── components/
        ├── ModeSelector.jsx
        ├── ScenarioSelector.jsx
        ├── ConversationView.jsx
        ├── SummaryPanel.jsx
        ├── ListeningStoryView.jsx
        ├── FooterNav.jsx      ← global sticky bottom nav (Home/Listening/Conversation/Translation/History)
        ├── VocabularyMatching.jsx
        ├── HistoryDashboard.jsx
        ├── SessionReview.jsx
        ├── HoverableText.jsx
        ├── EmailCapture.jsx  ← optional post-session signup, no-ops without VITE_FORMSPREE_URL
        ├── NavButton.jsx     ← shared prominent nav button (icon + label, 44px+, solid teal)
        ├── LoadingSpinner.jsx ← circular progress + word carousel, shown during story/response generation
        ├── TranslationView.jsx ← standalone bidirectional Spanish/English translator page (v1.0w+)
        ├── QuickTranslateModal.jsx ← in-story translate overlay, doesn't unmount ListeningStoryView (v1.0x)
        ├── RegenerateModal.jsx ← confirmation gate in front of Regenerate Story (v1.0x)
        ├── AboutModal.jsx    ← version badge → API usage/cost stats, backed by /api/usage-stats (v1.0x)
        ├── CustomTopicForm.jsx ← modal: user-entered topic + difficulty → generated story (v1.1a, SAC-071)
        └── ExplanationIcon.jsx ← ⓘ icon + expanding "how this sentence differs from English" panel (v1.2a, SAC-079)
```

**Note:** `ListeningHeader.jsx` (referenced in older Decisions Log entries below) was deleted in the v1.0u round — it only ever rendered the Regenerate button, which moved elsewhere. `FooterNav.jsx` is the current global nav.

**Non-standard note:** `index.html` lives at the root, not in `public/`, to work with Vite's default configuration.

---

## Tech Stack

| Layer | Choice | Notes |
|---|---|---|
| Frontend | React 18 + Vite | Dev server on localhost:5173 |
| Frontend hosting | Netlify (live, added v1.0k) | https://spanish-audio-chat.netlify.app — deployed via `netlify deploy --prod`, **not yet GitHub-connected** (manual deploy for now, see SAC-017) |
| Backend | Node.js + Express | Proxy pattern: all API calls via /api/* |
| Backend hosting | Railway (live, added v1.0k) | https://spanish-audio-chat-production.up.railway.app — deployed via `railway up`, **not yet GitHub-connected** (manual deploy for now, see SAC-017); `railway.json` overrides the build so Nixpacks doesn't also try to build the frontend |
| Database | IndexedDB (added v1.0j) | Local-only, per-browser session history (`src/db.js`); no backend involved, no shared/multi-user data |
| Story cache | SQLite (`story_cache` table, changed v1.0z, SAC-073 — was two in-memory `Map`s from v1.0r, Prompts #018-#019, before that a local JSON file in v1.0m; primary key changed from `scenario` alone to composite `(scenario, difficulty)` in v1.1d, SAC-076) | `getCachedStory`/`cacheStory` in `server.js`, keyed by `(scenario, difficulty)` (composite PRIMARY KEY, so a regenerate at the same difficulty `UPDATE`s the existing row rather than duplicating it, while a different difficulty for the same scenario gets its own independent row), backed by the same `./data/api_usage.db` SQLite file the usage-tracking log already used (`node:sqlite`'s `DatabaseSync` — no new dependency). **Survives a same-container restart, confirmed does *not* survive an actual Railway redeploy** — checked directly rather than assumed: `railway volume list` shows no persistent volume attached to this service, and a real back-to-back redeploy test (deploy, confirm all 8 scenarios warm, redeploy the identical code, check again) showed the second deploy started from `alreadyCached: 0` — same "ephemeral container filesystem" behavior already documented for the v1.0m filesystem-based story cache this replaced, and by extension something `api_usage.db`'s usage-tracking table has presumably always been subject to too, just never explicitly tested before. The actual, verified win is narrower than "persistent cache" alone suggests but still real: every fresh deploy now **self-warms automatically** within ~2.5 minutes via a non-blocking startup routine (see Key Architecture Decisions below), instead of relying on organic user traffic to eventually warm the old in-memory cache one scenario at a time. `/api/story-questions`'s `questionsCache` is a deliberate, disclosed exception — still the original in-memory `Map`, not persisted (though as of v1.1d/SAC-076 it's keyed on a composite `${scenario}|||${difficulty}` string, the same `(scenario, difficulty)` shape as `story_cache`, so switching difficulty can't serve questions written for a different difficulty's story) — since the user-facing win (Play becomes available fast) was already achieved by SAC-074 decoupling the loading spinner from that call entirely; persisting it too would be a reasonable future extension, not done this round. A cached scenario's second load is ~100ms end-to-end (verified live, both locally across a real process restart and on Railway within one deployed container). |
| Styling | Tailwind CSS | Light backgrounds per Vinay's preference |
| AI | Anthropic Claude API — claude-opus-4-8 | Current stable model (Jan 2025). **Always verify model string before assuming bugs are elsewhere.** Stale model strings have caused silent failures in the past. |
| Version control | GitHub (public, added v1.0k) | https://github.com/vvaidya98/spanish-audio-chat — push to `main` does **not** yet auto-deploy (Netlify/Railway are CLI-deployed, not GitHub-linked; see SAC-017) |
| Analytics | Console logging only (added v1.0k) | `src/analytics.js` `logEvent()` — no external provider set up yet |
| Email capture | Formspree (added v1.0k, inactive) | `EmailCapture.jsx` posts to `VITE_FORMSPREE_URL`; renders nothing if unset — no form created yet |

---

## Environment Variables

Never hardcode. Store in `.env` locally (gitignored) and in hosting platform's environment variable settings.

```
ANTHROPIC_API_KEY=sk-ant-your-key-here
PORT=3000
NODE_ENV=development
FRONTEND_URL=https://your-netlify-domain.netlify.app  (production only, set on Railway)
VITE_API_URL=https://your-railway-domain.up.railway.app  (frontend-only, Netlify build var; unset in dev)
VITE_FORMSPREE_URL=https://formspree.io/f/your-form-id  (frontend-only, optional — email capture is a no-op without it)
```

**Production values (v1.0k):** Railway has `ANTHROPIC_API_KEY`, `NODE_ENV=production`, `FRONTEND_URL=https://spanish-audio-chat.netlify.app`. Netlify has `VITE_API_URL=https://spanish-audio-chat-production.up.railway.app` (`VITE_FORMSPREE_URL` intentionally not set yet). **Caution for future sessions:** the local `.env` file has leading whitespace before some keys (confirmed on `ANTHROPIC_API_KEY=`) — a naive `grep '^KEY='` extraction will silently match nothing. Strip leading whitespace when scripting against this file.

---

## Key Architecture Decisions

- **Backend proxy pattern (required for CORS):** Initial HTML artifact hit browser CORS errors when calling Anthropic API directly. Solution: all API calls go through backend /api/* endpoints. Backend calls Anthropic API server-to-server (always allowed). API key stays in `.env` on backend, never exposed to browser.
- **Web Speech API (browser native):** No external speech libraries. Uses browser's built-in speech recognition (language: es-ES) and text-to-speech (rate: 0.8x for slower, learner-friendly pacing).
- **Feedback after every turn (not mid-conversation):** Claude provides Spanish response + English feedback after user finishes speaking. No interrupting mid-response.
- **Scenario flexibility:** Claude generates scenarios on-the-fly based on user selection, not a fixed list. Allows personalization in Phase 2+.
- **Language mix:** Claude always speaks Spanish (with slow rate). Feedback always in English (doesn't slow down learning). This aligns with Vinay's learning philosophy: understand natives + be understood, grammar polish later.
- **Listening-first, multi-turn conversation (added v1.0c):** Claude's Spanish text is hidden by default after every turn — the user must actively tap "Display text" to reveal it, or replay the audio at 1x/0.8x/0.6x speed. This forces listening comprehension before reading. Conversations run 5-8 exchanges (soft floor at 5 shows "End Conversation" alongside "Tap to Speak"; hard cap at 8 removes "Tap to Speak" and forces the end). Manual "Tap to Speak" / "Tap to Send" replaces the old auto-send-on-recognition-end flow, giving the user control over when their transcript is sent.
- **`/api/respond` returns structured error analysis (added v1.0c):** Backend prompts Claude to return a JSON object (`{ spanish, feedback, errors: [{ userSaid, corrected, explanation }] }`) instead of a `###`-delimited two-part string. `server.js` extracts the first `{...}` block via regex and `JSON.parse`s it, falling back to raw text if parsing fails. This feeds the end-of-conversation Summary view (`SummaryPanel.jsx`), which highlights each flagged error with its correction and explanation.
- **Two practice modes (added v1.0d):** `App.jsx` now gates on a `mode` state (`'conversation' | 'listening'`) before showing `ScenarioSelector`, which is reused unchanged for both modes (same 4 scenarios). Listening Mode routes to `ListeningStoryView.jsx` instead of `ConversationView.jsx`.
- **Story generation reuses conversation vocabulary (added v1.0d):** `/api/generate-story` asks Claude for a 150-200 word story (simple present tense, no subjunctive) that reuses vocabulary appropriate to the scenario, plus a full word-by-word `vocabulary` array (every distinct word, including function words like "el"/"la"/"de") for the tooltip feature. Needed `max_tokens: 4000` — the full-story vocabulary list for a ~150 word story runs to 50-90 entries and hit `stop_reason: 'max_tokens'` truncation (breaking `JSON.parse`) at the initial 1500 budget. `/api/story-questions` then generates 2-3 MCQ comprehension questions from the generated story text. Both reuse the same `extractJson()` regex+parse helper as `/api/respond`.
- **Word tooltips via plain-text tokenization (added v1.0d, component replaced in v1.0e):** Word tokenization (`text.split(/([^A-Za-zÀ-ÿ]+)/)`, capturing group keeps whitespace/punctuation as separate tokens) and lowercase vocab-map lookup. Words not found in the map render as plain text (no tooltip) rather than erroring — a defensive fallback in case the model misses a word. `WordTooltip.jsx` (v1.0d, word-only hover) was deleted and replaced by `HoverableText.jsx` (v1.0e) — see below.
- **Sentence-level hover + click-word tooltips via `HoverableText.jsx` (added v1.0e, SAC-014-A/C):** Single reusable component: hovering anywhere in the block highlights the whole unit (amber bg) and shows its `translation` prop below; clicking an individual word (independent of hover) shows a small tooltip with just that word's definition via `stopPropagation()` so it doesn't also toggle hover/parent click handlers. Reused for story transcript sentences, MCQ question text, and MCQ answer options — same component, different `text`/`translation`/`vocabulary` props each time. Root element is a `<div>` (not `<span>`) since every usage is block-level content; an early version wrapped it in a `<p>` for the MCQ question line, which is invalid HTML (`<div>` can't nest inside `<p>`) and triggered a React DOM-nesting warning — fixed by using a `<div>` wrapper instead.
- **Sentence-driven audio playback engine (added v1.0e, SAC-014-B):** `/api/generate-story` now returns a `sentences: [{ spanish, english }]` array instead of flat `story_spanish`/`story_english` strings — this is what actually drives playback: `ListeningStoryView.jsx` speaks one sentence's utterance at a time, waits `SENTENCE_GAP_MS` (2500ms) after each `onend`, then speaks the next. Playback state (`playStatus`: `idle | playing | gap | paused | finished`) plus `indexRef`/`rateRef`/`pausedRef`/`pauseContextRef` refs avoid stale-closure bugs in the async `onend`/`setTimeout` callbacks. Stop/Resume uses the native `speechSynthesis.pause()`/`.resume()` (works well in Chrome; per the existing cross-browser Web Speech caveat below, may be less reliable elsewhere) when pausing mid-utterance, but pausing during the inter-sentence gap just clears the pending `setTimeout` — Resume in that case jumps straight to the next sentence rather than replaying the finished one. Speed change mid-playback (1x/0.8x/0.6x) updates `rateRef` for future utterances and, if currently speaking, cancels and re-speaks only the *current* sentence at the new rate — Web Speech API has no way to change an in-flight utterance's rate or resume it at a new rate, so this is a deliberate, documented compromise: it does **not** restart the whole story from sentence 1 (satisfies the requirement), but it does restart the *current* sentence.
- **`/api/story-questions` now also returns per-question/option English translations + a `vocabulary` array (added v1.0e):** `question_english` and each option's `english` field feed the same `HoverableText` sentence-translation behavior. The separate `vocabulary` array (word → English) is deliberately independent from the story's own vocabulary — question words like "dónde"/"quién" often don't appear in the story text itself, so click-word definitions in MCQ context need their own source. `ListeningStoryView.jsx` merges `{ ...storyVocabMap, ...questionsVocabMap }` when rendering MCQ text so both sources are available.
- **StrictMode-safe mount effect (fixed v1.0f):** `main.jsx` wraps the app in `<React.StrictMode>`, which double-invokes effects in dev (mount → cleanup → mount) to help surface exactly this class of bug. `ListeningStoryView.jsx`'s mount effect called `loadStory()` unguarded, so both invocations ran their own fetch chain and, ~300ms after each resolved, called `speakSentenceAt(0)` — two independent playback sequences racing, canceling and restarting each other, heard as word repetition/stutter at story start. Fixed with the standard React pattern: a per-invocation `let stale = false` closure variable (set `true` in the effect's cleanup) checked via an `isStale()` callback before every `setState` call and before the `speakSentenceAt(0)` call inside `loadStory()` — the first (stale) invocation's late-arriving results are silently dropped once its cleanup has run, only the surviving invocation takes effect.
- **Hover shows translation-only badge, no Spanish highlight (changed v1.0f):** `HoverableText.jsx`'s hover behavior changed from "highlight the whole Spanish unit + show translation below" (v1.0e) to "leave the Spanish text visually unchanged, show the English translation as a small pill/badge below" (`bg-blue-100` rounded-full badge) — a deliberate legibility fix so the Spanish text is never visually altered by hovering. Click-word-for-definition is unaffected.
- **Story length increased to 7-10 sentences / 300-400 words (changed v1.0f):** `/api/generate-story`'s prompt now explicitly targets 30-45 words per sentence with descriptive detail and clause-joining words ("y"/"pero"/"porque"/"cuando") rather than short one-action sentences — the first attempt at just raising the sentence-count target (without this per-sentence word-count guidance) produced 10 short sentences totaling only ~70 words, since "simple, beginner-friendly" sentences naturally default to short. `max_tokens` bumped 4500 → 7000 for the larger vocabulary array a longer story requires. Tested output: 278 words / 10 sentences with a real narrative arc (beginning/middle/end), reasonably close to target.
- **Sentence length reduced back to 15-25 words (changed v1.0g):** v1.0f's 30-45 word sentences turned out to be too much to digest in one auto-paused chunk in real use. Same lesson as v1.0f applied in reverse: a first prompt revision just lowering the target words-per-sentence undershot badly (~10 words/sentence, 99 words total) — the model defaults toward short sentences unless explicitly, forcefully told otherwise. Needed a second revision with a concrete before/after example embedded in the prompt itself ("'Carlos entra en un restaurante bonito y busca una mesa.' (10 words) is too short; expand it... (19 words)") before it reliably landed in the 15-25 word range (tested: 18-20 words/sentence, 169 words total).
- **Smooth mid-play speed change via `onboundary` tracking (changed v1.0g, replaces the v1.0e/f documented restart-current-sentence compromise):** Users found the old "cancel + re-speak the whole current sentence at the new rate" approach audible as a stutter/repeat. Fix: `speakSentenceAt` and `handleSpeedChange` both attach `utterance.onboundary`, which fires per-word during speech (in browsers/voices that support it — not universal, see Web Speech API caveat below) and records the char index of the most recently completed word into `lastWordCharIndexRef`. On a mid-play speed change, `ListeningStoryView.jsx` slices the sentence's text from `speakOffsetRef.current + lastWordCharIndexRef.current` onward and speaks only that remainder at the new rate — genuinely resuming, not restarting. `speakOffsetRef` accumulates across repeated speed changes within the same sentence so a second change resumes from the first change's resume point, not from the sentence start. Verified in testing: a speed change after ~4 of 20 words produced a 16-word follow-up utterance (not 20). Honest limitation: if a browser/voice never fires `onboundary`, `lastWordCharIndexRef` stays 0 and the change falls back to replaying the whole sentence — graceful degradation to the old v1.0e/f behavior, not a crash.
- **Transcript redesigned around explicit per-sentence icons, not hover (changed v1.0g):** The transcript's hover-to-translate behavior (v1.0f) is replaced there by numbered sentences with two icon buttons each: 🔊 plays just that sentence (`playTranscriptSentence`, fully independent of the main sequential playback engine — tracks its own `transcriptPlayingIdx`, cancels whatever else is speaking first so only one audio source is ever active) and 🌐 toggles that sentence's English translation via `openTranslationIdx` (a single nullable value, so opening one always closes any other). `HoverableText` gained a `showHoverTranslation` prop (default `true`, so MCQ question/option rendering is unaffected) — set `false` for the transcript's Spanish text so only its click-word-for-definition behavior remains active; hover no longer does anything there. The old always-visible full-story English paragraph block was dropped as redundant now that per-sentence translation is available on demand.
- **Main controls redesigned as icons + progress bar (changed v1.0g):** Start/Stop-Resume/Restart text buttons replaced with ⏮ (restart) / ▶‑⏸ (combined play-pause toggle, same underlying logic as the old Start+Stop/Resume, just consolidated into one icon) / ⏭ (new: jump straight to the last sentence via `handleJumpToEnd`). A progress bar plus "Sentence X of Y" label was added, computed as `(currentIndex / totalSentences) * 100`, reaching 100% only in the `finished` state — deliberately coarse (per-sentence, not audio-time-based) since Web Speech API exposes no utterance duration/progress to drive a smoother fill.
- **Token-guarded speaking engine, replacing native pause() (fixed v1.0h):** Two related real bugs found in the v1.0g playback engine. (1) `speechSynthesis.pause()` is known to sometimes let the current utterance finish before actually pausing in real browsers — replaced with `cancel()` (synchronous, immediate) everywhere the old code called `.pause()`, combined with the same `onboundary`-tracked resume logic used for speed changes so "Resume" still continues roughly where it left off rather than restarting. (2) `cancel()`ing an in-progress utterance doesn't reliably suppress its `onend`/`onerror` in every browser — some fire them anyway. The v1.0g `handleSpeedChange` canceled the *old* utterance (whose `onend` closure still captured the old `idx` and called `handleSentenceUtteranceEnd(idx)`) right before speaking a *new* one; if the old utterance's `onend` fired anyway, it scheduled a stray duplicate "advance to next sentence" `setTimeout` alongside the legitimate one — audible as skipped/repeated sentences, especially with rapid speed changes. Fixed with a token-guard: every utterance is tagged with `++utteranceTokenRef.current` at creation, and its `onboundary`/`onend`/`onerror` handlers check their captured token against the ref before doing anything, so a stale utterance's late-firing event becomes a no-op once something newer has superseded it. Verified specifically against a mock that deliberately reproduces the buggy cancel-still-fires-onend behavior (not reproducible with a "well-behaved" mock) — confirmed the fix holds even then. `speakSentenceAt`, `resumeSentenceFromBoundary` (new, factors out the shared "speak the sentence's remaining text" logic used by both speed-change and pause-resume), and `playTranscriptSentence` all share the one `utteranceTokenRef`, since only one audio channel (`synthRef.current`) exists regardless of which of them is currently speaking.
- **Navigation moved to `ListeningHeader.jsx` at the top of the view (added v1.0h):** New small header component with ← Back, 📋 Change Mode, and 🔄 Diff Scenario buttons, rendered above both the loading state and the loaded story (previously navigation was a single "← Back to Scenarios" button at the bottom, now removed). "Diff Scenario" is new: `DEFAULT_SCENARIOS` was changed from a private constant to a named export in `ScenarioSelector.jsx` so `App.jsx` can pick a random *different* scenario and call `setScenario()` directly, skipping the picker screen. `App.jsx` renders `<ListeningStoryView key={scenario} .../>` — the `key` change forces a full unmount/remount on scenario change, which is what actually resets all of `ListeningStoryView`'s internal state and refs (playback position, transcript toggles, answered questions, the token/pause refs, etc.) rather than needing to manually reset each one.
- **Story vocabulary broadened, sentences shortened again (changed v1.0h):** `/api/generate-story`'s prompt now explicitly asks for vocabulary beyond the obvious handful of words for a scenario (e.g., for "Ordering at a Restaurant": not just pollo/arroz/agua every time, but pescado/verduras/ensalada/vino/café and varied verbs like probar/recomendar/disfrutar) — this reverses the original v1.0d design intent ("Reuse vocab: Stories use same words as Conversation Mode scenarios, reinforces learning"), pivoting from reinforcement to breadth based on real usage feedback; worth knowing if a future prompt asks to reconsider this again. Sentence length reduced from 15-25 words (v1.0g) to 10-15 words (~100-150 total, down from ~200-250) — unlike the two prior length changes (v1.0f, v1.0g), this one hit the target range on the very first prompt attempt (11-13 words/sentence, 102 words total), likely because the prompt included a calibrated example sentence from the start instead of only a numeric target.
- **Design token system: CSS variables wired into Tailwind theme (added v1.0i):** `src/index.css`'s `:root` defines the actual color/radius values as CSS custom properties (`--color-primary`, `--color-surface`, `--color-text`, etc. — teal primary, coral secondary, plus semantic success/danger/warn), and `tailwind.config.js`'s `theme.extend` maps Tailwind color/radius/fontSize names onto `var(--color-*)` (e.g. `colors.primary.DEFAULT = 'var(--color-primary)'`). This gives genuine single-source-of-truth CSS variables (satisfies "no hardcoded colors") while keeping normal Tailwind utility ergonomics (`bg-primary`, `text-ink-muted`, `rounded-card`) everywhere in JSX — no component reaches for a raw hex or a bespoke Tailwind palette color anymore. Typography scale added as named `fontSize` keys (`text-heading-1`=28px/700, `text-heading-2`=18px/700, `text-body`=16px, `text-small`=14px) rather than reusing Tailwind's default `text-2xl`-style scale, so the four sizes read as an intentional hierarchy in the markup. Dark mode was scoped out — CLAUDE.md's existing design philosophy already commits to light backgrounds as a firm preference (not "if applicable"), and no dark-mode infrastructure existed to extend.
- **`VocabularyMatching.jsx`: click-to-pair game (added v1.0i):** New component, rendered in `ListeningStoryView.jsx` right after the Comprehension Check section once `playStatus === 'finished'`. Takes a `words` prop (`{word, english, difficulty}[]`, 5-10 entries from `/api/story-questions`'s new `matchingWords` field) and locally shuffles+letters (a-j) the English side once via a lazy `useState` initializer (so re-renders don't reshuffle). Two independent `selectedWordIdx`/`selectedOptionIdx` pieces of state let the user start a pairing from either column; once both are set, `attemptMatch` checks correctness, adds the word index to a `matched` Set on success, and shows a transient (1.2s auto-clearing) green/red feedback banner either way. Matched entries get a persistent checkmark and become disabled/non-interactive. A completion banner appears once `matched.size === words.length`.
- **`/api/story-questions` gained `matchingWords` (added v1.0i):** Same call as the existing MCQ generation (no extra round-trip) — the prompt now also asks for 5-10 words *verbatim from the story text* with an explicit easy/medium/hard mix, returned as a third array alongside `questions` and `vocabulary`. `max_tokens` bumped 2500 → 3000 for the added content.
- **Local session history via IndexedDB (added v1.0j, SAC-013):** New `src/db.js` — minimal wrapper (`openDB`/`saveSession`/`getAllSessions`/`generateSessionId`) around a single `sessions` object store (keyPath `id`), with indexes on `mode`, `scenario`, and `timestamp` for the History Dashboard's filtering. This is genuinely local-only — nothing here touches `server.js` or the Anthropic API; session data never leaves the browser. `ConversationView.jsx` saves on "End Conversation" (exchanges, error count, computed duration via a `sessionStartRef` set at mount); `ListeningStoryView.jsx` saves on the header's "← Back" click (`handleBackWithSave`, only fires if `story` has actually loaded — nothing to save otherwise) — captures story, questions, user's MCQ answers, and vocabulary-match progress at that moment. Both wrap the save call in a `.catch(console.error)` rather than blocking navigation if IndexedDB is unavailable (e.g. very old browsers, some privacy-mode configurations) — a failed save degrades to "no history for this session," not a broken app. **Known gap:** a session is only saved on the explicit "leave" action (End Conversation / Back) — closing the tab or navigating away mid-session mid-story loses that session's data; no `beforeunload` handler exists to catch that case.
- **`VocabularyMatching.jsx` gained `onProgressChange` (added v1.0j):** Fired with `(matchedCount, total)` whenever a correct match is made, letting `ListeningStoryView.jsx` track match progress for saving *without* lifting the `matched` Set itself out of the child — the component stays self-contained for its own interaction logic, just reports upward. Session storage keeps a `matchedCount` and the full `matchingWords` list, not which specific words were matched — `SessionReview.jsx`'s Listening view shows "matched N of M" plus the full word list, not per-word matched/unmatched status. A deliberate scope-trim, not an oversight — flagged here in case a future round wants per-word review detail.
- **`HistoryDashboard.jsx` / `SessionReview.jsx` (added v1.0j, SAC-015):** Dashboard loads all sessions once via `getAllSessions()`, sorts newest-first client-side, and derives stats/filters/pagination (10 per page) from that in-memory array — no re-querying IndexedDB per filter change, since the whole session set is expected to stay small (personal-use app, not a shared multi-tenant system). Mode and scenario filters are independent `useState` values combined in a single `useMemo`. `SessionReview.jsx` branches entirely on `session.mode`: `ConversationReview` owns its own `exchangeIdx` state for Previous/Next navigation (mirrors `SummaryPanel.jsx`'s error-highlight styling); `ListeningReview` is stateless, just renders the three static sections (transcript, MCQ results, vocab summary) directly from the saved session object.
- **Scenario picker: "Choose One for Me" + 8 scenarios (changed v1.0j, SAC-015):** The old "Different Scenarios" button was dead weight — it called `/api/initiate` with a throwaway prompt and then just reset to the same static `DEFAULT_SCENARIOS` list, never actually using the response. Replaced with a pure client-side pick-and-start: picks a random entry from `DEFAULT_SCENARIOS` and calls `onSelectScenario` directly, skipping the confirm screen entirely (unlike clicking a scenario card, which still goes through the confirm step). List expanded from 4 to 8 (added At the Airport/Hotel, At a Pharmacy/Doctor, Shopping in a Store, Asking for Help/Emergency) — `ScenarioSelector.jsx` no longer needs its own `scenarios` state since the list is no longer mutable at runtime, so `DEFAULT_SCENARIOS` (the named export other components already import) is rendered directly.
- **Production deploy: CLI-based, not GitHub-connected (added v1.0k, SAC-003–007):** Backend deployed to Railway via `railway up` (uploads the local working tree directly); frontend deployed to Netlify via `netlify deploy --prod --dir=dist` (uploads a pre-built `dist/`). Neither is linked to the GitHub repo for auto-deploy-on-push yet — that's tracked separately as SAC-017 since wiring it up requires the Netlify/Railway GitHub App OAuth flow (browser-interactive, same class of blocker as the CLI logins below), not just CLI commands. Until SAC-017 ships, a code change requires a manual `git push` **and** a manual `netlify deploy`/`railway up` to actually go live — pushing to GitHub alone does nothing.
- **Railway needs its own build override (`railway.json`, added v1.0k):** Railway's Nixpacks builder auto-detects a `build` script in `package.json` and runs it unconditionally — since this repo's `npm run build` is the *frontend's* Vite build, Railway tried (and failed, missing `terser`) to build the frontend on a service that only needs to run `npm run backend`. Fixed with `railway.json` (`build.buildCommand` set to a no-op echo, `deploy.startCommand` set explicitly to `npm run backend`) rather than removing/renaming the root `build` script, since local `npm run build` still needs to work for Netlify's build step.
- **`src/api.js`: single `VITE_API_URL`-aware fetch wrapper (added v1.0k, SAC-009):** All 4 `fetch('/api/...')` call sites (2 in `ConversationView.jsx`, 2 in `ListeningStoryView.jsx`) now go through `apiFetch(path, options)`, which prefixes `path` with `import.meta.env.VITE_API_URL` (empty string if unset). In dev this env var stays unset, so requests remain relative and go through the existing Vite proxy to `localhost:3000`; the Netlify production build sets it to the Railway URL at build time. This is a build-time substitution (Vite inlines `import.meta.env.*` at build), not a runtime lookup — changing `VITE_API_URL` on Netlify requires a rebuild+redeploy, not just a restart.
- **Analytics: console-only `logEvent()`, no provider yet (added v1.0k, SAC-016):** `src/analytics.js` exports one function; call sites (`page_view` in `App.jsx`, `session_started`/`session_completed` in both view components, `history_dashboard_viewed` in `App.jsx`) are already wired so that swapping the function body for a real provider (Plausible was the one discussed) later touches one file, not every call site. Deliberately not using `NODE_ENV`/`import.meta.env.PROD` to suppress dev logging — the console output *is* the product for this round, per the prompt's own "just log to console" recommendation for launch.
- **Email capture: `EmailCapture.jsx`, ships inactive (added v1.0k, SAC-016):** Renders a small form (email input + Formspree POST) after a completed Conversation (`SummaryPanel.jsx`) or Listening (`ListeningStoryView.jsx`, once `playStatus === 'finished'`) session. Reads its endpoint from `VITE_FORMSPREE_URL` and renders `null` entirely if that's unset — Vinay chose to skip creating a Formspree form this round (it requires his own email to verify), so this ships as dead-but-harmless code rather than a half-built feature blocking the rest of the deploy. Verified via Playwright that the form genuinely doesn't render (0 `input[type=email]` elements) with the var unset, so there's no broken-looking empty box.
- **`NavButton.jsx`: single shared prominent nav component (added v1.0l, SAC-018):** Replaced every barely-visible text-link nav instance (Back / Change Mode / Diff Scenario / Back to History) with one component (icon + label, solid `bg-primary` teal fill, `min-h-[44px]`, `hover:bg-primary-hover`) used identically across `ListeningHeader.jsx`, `HistoryDashboard.jsx`, `SessionReview.jsx`, and — newly — `ConversationView.jsx`, which never had a header nav row before this round (only a bottom "New Topic" button). Building that new header required adding `onChangeMode`/`onDifferentScenario` props to `ConversationView` (wired from `App.jsx`, mirroring `ListeningStoryView`'s existing pattern) and adding `key={scenario}` to its `<ConversationView>` usage so "Diff Scenario" actually remounts and resets internal state, the same technique `ListeningStoryView` already relied on.
- **Loading spinner replaces the static hourglass (added v1.0l, SAC-021):** New `LoadingSpinner.jsx` — an SVG circular progress ring whose fill eases toward 95% over an `estimatedMs` duration (never claims 100% before the real response actually arrives) plus a single-word carousel (`src/scenarioVocab.js`, 6 themed words per scenario) that rotates every ~1.7s through 4 alternating CSS entrance animations. Used for `ListeningStoryView.jsx`'s full story-generation wait, and for `ConversationView.jsx`'s *initial* load only — deliberately not every mid-conversation "Processing..." turn, which keeps its old lightweight icon+text treatment, since a full spinner+carousel repeating every single turn would be visually excessive for what's normally a ~2s wait. Real bug hit while wiring this into `ConversationView.jsx`: `currentState` initializes to `'starting'` but `startConversation()`'s very first line synchronously flips it to `'processing'` before the first paint, so gating the spinner on `currentState === 'starting'` alone meant it never actually rendered in practice (confirmed via a screenshot still showing the old hourglass) — fixed with a derived `isInitialLoad = exchangeCount === 0 && (currentState === 'starting' || currentState === 'processing')`, since distinguishing "still on the opening API call" from "mid-conversation processing" requires exchange count, not `currentState` alone.
- **Story completion: toggle-based, not auto-shown (changed v1.0l, SAC-019):** `ListeningStoryView.jsx` no longer auto-renders the Comprehension Check when the story finishes. A new `showMCQ` state pairs with the pre-existing `showTranscript` state behind two side-by-side toggle buttons (teal-filled when active, light when inactive) that the user must explicitly tap; both can be open at once. Vocabulary Matching still auto-shows on finish unchanged — the spec only covered MCQ + transcript, not vocab.
- **Vocabulary match feedback: inline animations + synthesized beep, no button (rewritten v1.0l, SAC-020):** `VocabularyMatching.jsx`'s old "✓ Correct! / ✗ Try again" banner (which shifted the whole card's layout on every attempt) is gone. Correct matches get a persistent animated green checkmark directly on the word row (`useEffect` scoped `@keyframes`, no layout shift); wrong matches get a 1.5s red X + red-bordered flash on *both* the attempted word and the attempted option (clearer feedback than flashing only one side), plus an audible beep synthesized via Web Audio API (`OscillatorNode`, square wave, 800Hz, 200ms — no MP3 asset needed, gracefully no-ops if `AudioContext` is unavailable). Fixed a real pre-existing React anti-pattern surfaced by testing this change: the correct-match path called the parent's `onProgressChange` from inside `setMatched`'s updater function, which triggers "Cannot update a component while rendering a different component" warnings — moved the call into a `useEffect` keyed on `matched` instead, the correct place for a parent-notifying side effect.
- **Mobile-specific sizing + a real mobile-only layout bug found via screenshot (added v1.0l, SAC-022):** Playback control icons and their row now stack vertically below `sm:` breakpoint with slightly larger touch targets on mobile (48px vs. 44px desktop); speed buttons gained `min-w-[44px]` and more mobile padding. Screenshot-based testing at 390px width (not just a class-name read) caught a genuine bug: `VocabularyMatching.jsx`'s word buttons used `flex items-center justify-between` with no wrapping, so a longer Spanish word plus its difficulty badge would overflow the button's right edge and visually clip/overlap — invisible unless you actually look at a rendered screenshot at a narrow width. Fixed by making both word and option rows `flex-wrap`, letting the badge drop to its own line instead of overflowing.
- **Compact combined header (changed v1.0l, SAC-023):** `ListeningStoryView.jsx`'s separate "Topic: {scenario}" card plus large centered "Listen carefully" heading (~140px+ combined) replaced with one `border-l-4 border-primary` accent-bordered block (bold scenario name, "Listen carefully" as a small subtitle, ~70px). `ConversationView.jsx` got the equivalent compact-header treatment for cross-screen consistency.
- **Audio autoplay: detect-and-fallback, not a definitive fix (added v1.0l, SAC-024; superseded in Listening Mode by v1.0s, SAC-037):** Root cause (mobile browser user-activation policies for `speechSynthesis.speak()`) still can't be confirmed from this environment — see the v1.0k-round Decisions Log entry: Playwright's WebKit test build has no Web Speech API implementation at all, so real device behavior remains unverified. Original v1.0l approach was detection, not prevention: ~900ms after a story's opening `speak()` call, check whether the engine actually reports `speaking`, and show a "🔊 Tap to Play" banner if not. **Prompt #020 (SAC-037) removed this mechanism entirely from `ListeningStoryView.jsx`** rather than keeping it alongside the new mandatory "Tap to Begin" gate — once every `speak()` call is structurally guaranteed to originate from a real click, there's no more silent-failure case left for the detector to catch, so keeping the dead code around would have just been confusing. `ConversationView.jsx` never had this detect-and-fallback mechanism to begin with and still doesn't have any gate — see Known Issues, Issue 2.
- **Mode order: Listening first (changed v1.0m, SAC-025):** `ModeSelector.jsx`'s two buttons swapped — purely a JSX reorder, `onSelectMode('listening')`/`onSelectMode('conversation')` unchanged, no styling touched.
- **Sentence jump markers, deliberately smaller than the 44px convention (added v1.0m, SAC-026):** New `handleJumpToSentence(idx)` in `ListeningStoryView.jsx` mirrors the existing `handleJumpToEnd`'s cancel-and-restart pattern, parameterized by index. Rendered as a `flex-wrap` row of 32×32px numbered buttons below the progress bar — smaller than this project's usual 44px tap-target rule, a deliberate exception since the spec explicitly called for "28-32px square" for this specific dense multi-button row (a story can have up to 10 sentences; 44px buttons for all of them would force multiple wrapped rows even on desktop).
- **Vocabulary matching: one-at-a-time with example context (rewritten v1.0m, SAC-027 + SAC-028 together):** `VocabularyMatching.jsx` moved from the v1.0i/v1.0l all-at-once two-column grid to one word at a time, sorted easy→medium→hard once via `useMemo` on mount. Each word's 4-5 options (1 correct + up to 4 distractors) are drawn from the *other* words in the same list (`buildOptions`), so no external distractor pool is needed and the option set naturally shrinks gracefully for shorter word lists. Correct match plays a distinct success tone (880Hz sine) — distinct from the wrong-match tone introduced in v1.0l (220Hz square, lowered from v1.0l's 800Hz to read as more clearly "wrong" against the new "right" tone) — then, if the word has example data, shows a phrase + sentence box (Spanish + English) for 600ms in place of the "✓ Correct!" text before auto-advancing; without example data it's just the 600ms "✓ Correct!" banner. Incorrect match plays the wrong tone, shows "Try again!", and deliberately leaves the word in place with all options still clickable — no option gets disabled, so a repeat wrong click is possible (matches the spec's "user can retry" language literally, not "user can retry among remaining options"). Backend: `/api/story-questions`'s prompt now asks for `examplePhrase`/`examplePhraseEnglish`/`exampleSentence`/`exampleSentenceEnglish` per matching word — a short phrase and a full sentence, each in a context *different* from the story itself, so the word is reinforced in a genuinely new setting rather than just echoing the story text back; `max_tokens` raised 3000 → 4500 to fit the roughly 4x larger per-word payload.
- **Story caching: file-based, explicitly scoped to `/api/generate-story` only, ephemeral on Railway (added v1.0m, SAC-029):** New cache helpers in `server.js` (`.cache/stories.json`, keyed by scenario name) — `getCachedStory`/`cacheStory` wrap `readStoriesCache`/`writeStoriesCache`, which fail soft (log + return `{}`/no-op) rather than crash the request if the filesystem is unwritable. `/api/generate-story` accepts `regenerate` in the request body: falsy checks cache first and returns in ~150ms on a hit (verified via direct `curl` timing, isolated from the rest of the load flow); a miss or `regenerate: true` always calls Claude fresh and re-caches. Deliberately *not* caching `/api/story-questions` — out of this SAC's stated scope — so a "second load" of an already-cached scenario is faster but not instant, since the (now heavier, post-SAC-028) questions call still runs fresh every time; confirmed this wasn't a caching bug by isolating the cached endpoint's own timing before drawing that conclusion. **Known limitation, disclosed not hidden:** this is a local JSON file on the container's filesystem — Railway's filesystem doesn't persist across redeploys/restarts, so the cache resets whenever the backend redeploys; it speeds up repeat requests within one running server process, not indefinitely. Frontend: `loadStory(isStale, regenerate)` threads the flag through; new `handleRegenerateStory()` resets all playback/session UI state before calling `loadStory(() => false, true)`; new "🔄 Regenerate Story" `NavButton` in `ListeningHeader.jsx` via an optional `onRegenerate` prop, only passed once a story has actually loaded (not during the initial loading state).
- **Speed labels: descriptive, not raw multipliers; default lowered 0.8x → 0.5x (changed v1.0m, Prompt #012 Task 1):** `ListeningStoryView.jsx`'s speed buttons relabeled `Slow`/`Normal`/`Fast` mapped to `[0.6, 0.5, 0.4]` (down from `[1.0, 0.8, 0.6]`), each with a `title` tooltip showing the real multiplier. Per user feedback, perceived speed and the raw number don't line up for this TTS voice — 0.6x reads as "normal," 1.0x as unusably fast — so the default moved to what's now labeled "Normal" (0.5x) rather than the old 0.8x default, which was already on the too-fast side of that perception.
- **"Back to Stories" duplicates "Back" on purpose (changed v1.0m, Prompt #012 Task 2):** `ListeningHeader.jsx`'s third button (formerly "Diff Scenario," auto-picked a random different scenario and jumped straight into it, skipping the picker) is now "Back to Stories," wired to the same `onBack` handler as the actual "Back" button. The two buttons are now functionally identical in Listening Mode — an explicit, literal instruction ("call onBack() directly... This already saves the session and navigates back"), not an accidental duplication. Scoped to Listening only; Conversation Mode's separate "Diff Scenario" button (its own auto-pick-and-restart, from the v1.0l NavButton round) is untouched — the prompt's file/testing scope never mentioned it.
- **App renamed "Spanish Audio Chat" → "Conversation Amigo" (changed v1.0m, Prompt #012 Task 3):** Brand text only — `index.html` (`<title>`, new `<meta name="description">`), `App.jsx`'s `<h1>`, `README.md`'s title, `package.json`'s `name` field (`spanish-audio-chat` → `conversation-amigo`, `package-lock.json` synced via `npm install` rather than hand-edited). **Deliberately unchanged:** the actual Netlify URL, Railway URL/service name, GitHub repo, local folder name, and the IndexedDB database name in `db.js` (still literally `spanish-audio-chat`) — none of those were asked to be renamed, and doing so would break live links/bookmarks or require re-provisioning infrastructure. `grep -rn "Spanish Audio Chat" src/` confirmed zero remaining matches after the change.
- **Loading spinner: fake progress bar and word carousel both removed (rewritten v1.0m, Prompt #012 Task 4):** `LoadingSpinner.jsx` rewritten from scratch — gone: the SVG circular progress ring that eased toward 95% over an estimated duration (added v1.0l, SAC-021), the percentage display, and the scenario-word carousel (also v1.0l) that rotated through `scenarioVocab.js`'s themed word pools. None of those were literally "fake" except the percentage, but the prompt's "Keep" list (spinner icon + label + container only) and target pseudo-code didn't include the carousel either, so it was read as in-scope for removal too rather than kept alongside the new minimal design. Replacement: a plain `animate-spin` CSS ring, the unchanged `label` prop, and a new `estimateText` prop (default `"This usually takes 10-15 seconds"`) instead of hardcoding that string at every call site — `ListeningStoryView.jsx` keeps the 10-15s default (accurate for its real story-generation wait), but `ConversationView.jsx`'s shorter ~2-5s initial load now passes `"This usually takes a few seconds"` instead, since showing "10-15 seconds" there would itself have been a dishonest estimate — directly against this task's own point. `src/scenarioVocab.js` deleted as dead code once nothing referenced it anymore.
- **Clarity Mode: boolean toggle → 5-level dropdown (changed v1.0x, SAC-069, built in a parallel Claude Code session):** `clarityMode` (boolean) became `clarityLevel` (`'off'|'low'|'medium'|'high'|'ultra'`), with `CLARITY_PAUSE_MS` changed from a single constant (130ms) to a per-level map (`{ off: 0, low: 80, medium: 130, high: 180, ultra: 250 }`) — the old 130ms "on" behavior is preserved exactly as the new "medium" level, so existing users' sense of what Clarity Mode sounds like doesn't shift under them. `speakClaritySegment` and `speakSentenceAt` thread the resolved `pauseMs` through as a parameter instead of reading the module-level constant directly, and the mid-playback-changeable-value pattern (state + a mirroring ref so the async speak chain always reads the current value) carries over unchanged from the original boolean version. The segment-chain playback mechanism itself (split on connector words, chain per-segment utterances) and its documented scope trim (mid-sentence pause/resume/speed-change restarts the current sentence rather than resuming mid-segment) are both unchanged — only the pause duration became selectable.
- **TTS voice preference: Colombian/Latin American Spanish before Spain Spanish (changed v1.0x, SAC-070, built in a parallel Claude Code session):** `applySpanishVoice()` in `speechUtils.js` previously picked the first `es-ES` voice, falling back to any `es-*`. Now walks an explicit preference list (`es-CO → es-419 → es-MX → es-US → es-ES`) and takes the first match, only falling back to "any `es-*`" if none of those specific tags are available — which voices actually exist is entirely OS/browser-dependent, so this only controls *preference order* among whatever's installed, it can't conjure a Colombian voice into existence on a system that never shipped one. Also sets `utterance.lang` to the *selected* voice's own lang (previously left at the hardcoded `'es-ES'` regardless of which voice got picked) and adds a one-time `console.log` reporting which voice/lang actually got used, so "which voice is this?" is answerable without digging through devtools. Investigated whether the Web Speech API exposes any "clarity"/"articulation" parameter beyond rate/pitch/volume — it doesn't; pitch was deliberately left at its default (no documented basis that changing it improves clarity, real risk of making it worse) rather than tuned speculatively. The actual clarity lever this round added is SAC-069's pause duration, not a voice-parameter change.
- **Play button pulse: hardened to survive a full page reload, not just component re-renders (changed v1.0x, SAC-048 follow-up, built in this session):** The original SAC-048 pulse (v1.0u) used plain `useState(true)` for `isFirstLoad`, reset on any full remount. Investigated a reported "pulse never stops on localhost, only on production" complaint by live-testing against the real dev servers with a headless-Chrome driver (not guessing from reading the code): the originally-suspected cause — React Fast Refresh/HMR resetting `isFirstLoad` on file save — did **not** reproduce across editing the component itself, a non-component dependency, a sibling component, or a rapid 5-edit burst; state and a JS-context marker both survived every case, confirming Fast Refresh correctly preserves state here despite the `forwardRef` wrapping. What *did* reproduce: a genuine full page reload (not HMR) — a real, if less exotic, thing to hit repeatedly during active dev testing. Fix: `isFirstLoad` now lazily initializes from `sessionStorage`, keyed per scenario (`listening_played:<scenario>`), written by both the Play-click and Regenerate paths (`hasAlreadyPlayed`/`markAsPlayed` helpers in `ListeningStoryView.jsx`). This is deliberately scoped to *this scenario's* tab session, not "ever, anywhere" — verified live (real Chrome against both localhost and, after deploy, production) that a hard reload of the *same* scenario keeps the pulse off, while picking a *different*, never-played scenario still pulses fresh, matching the original "story loads → pulses" spec rather than silently suppressing it app-wide after one click.
- **First-load spinner decoupled from Comprehension Check data (changed v1.0y, SAC-074):** `loadStory()`'s two Claude calls (`/api/generate-story`, `/api/story-questions`) were both awaited sequentially before `setLoading(false)`, even though `/api/story-questions`'s MCQ/vocab data isn't read anywhere until `playStatus === 'finished'` — minutes into playback for a 10-sentence story. Split the function: `setLoading(false)` (and `logEvent('session_started', ...)`) now fire as soon as `/api/generate-story` resolves, and `/api/story-questions` runs afterward in its own try/catch that populates `questions`/`questionsVocab`/`matchingWords` whenever it finishes, without touching the top-level `error` state on failure — a story that already loaded fine shouldn't get hidden behind an error banner because the background fetch had trouble. Applies to Regenerate too, since `handleRegenerateStory()` calls the same `loadStory()`. `LoadingSpinner`'s Listening-mode `estimateText` prop (previously the shared default, "30 to 40 seconds," calibrated for the old combined wait) is now explicitly overridden at this call site to "10 to 20 seconds," matching the shorter actual wait — same honesty principle as the original Prompt #012 design (never show an estimate calibrated for a wait the code no longer has). **Important, disclosed scope note:** this fixes the real bottleneck, but Prompt #027's own literal spec (pre-synthesizing sentence 0's TTS during loading) was not built as written — see Decisions Log for why that diagnosis was incorrect and the literal spec would have reintroduced the SAC-037 mobile-autoplay bug.
- **CRITICAL FIX: production build now actually builds React in production mode (fixed v1.0y, discovered while verifying SAC-074):** Every prior production deploy — Netlify's own server-side build *and* every local `npm run build` used for manual deploys throughout this project's entire history — resolved `react`/`react-dom` to their **development** builds, not production. `react-dom`'s package itself branches dev-vs-prod via `process.env.NODE_ENV` at bundle time (a `require`-time conditional inside the package, independent of Vite's own `--mode`/`import.meta.env.MODE`), and nothing in this repo ever set that explicitly — so whatever ambient `NODE_ENV` the build process happened to have (unset locally; Netlify's build environment must also leave dev-dependencies installable, so it isn't `production` there either) determined the outcome, and it was never `production`. Confirmed two ways: the built bundle contained React's dev-only `console.error`/`console.warn` warning-printer strings (absent from a real production bundle), and forcing `NODE_ENV=production` before the exact same `vite build` command dropped the minified bundle from 447KB to 204KB (204KB is correct/production-sized). The functional consequence is severe, not cosmetic: React's dev-mode StrictMode behavior (mount → cleanup → mount, double-invoking every mount-only effect) was active **in production**, for the entire time this app has been live — every `useEffect(() => {...}, [])` mount effect across the app (most importantly `ListeningStoryView.jsx`'s `loadStory()` call) fired twice per real page load, silently doubling every Claude API call driven by a component mount. Confirmed via a controlled, disambiguated test against the live production URL: one real, unambiguous scenario-card click fired two identical `/api/generate-story` POST requests within 1ms of each other. This is what was actually inflating the SAC-074 investigation's background-fetch timing (see the corrected note on that entry above), and had been silently doubling Claude API cost on every Listening-mode load since the very first production deploy (v1.0k) — the existing `isStale()` guards (built specifically to handle StrictMode's *dev*-only double-invoke) happened to make this non-catastrophic functionally, since the guards are equally effective regardless of *why* a second invocation occurs, but they don't stop the wasted second API call from actually being made and billed. **Fix:** added `cross-env` as a devDependency and changed `package.json`'s `build` script to `cross-env NODE_ENV=production vite build` — this is cross-platform (works identically on this Windows dev machine and Netlify's Linux build image) and doesn't depend on the ambient shell's `NODE_ENV` being anything in particular. Verified: 0 dev-warning strings in the rebuilt bundle, and a repeat of the exact same disambiguated single-click test now shows exactly one `/api/generate-story` and one `/api/story-questions` request, both locally (via `vite preview`, decoupled from Netlify's CDN entirely, to confirm this was a build artifact and not a hosting-layer issue) and — after redeploying — live on production. **Lesson for future rounds:** "the build succeeded and the app works" was never a strong enough check for this class of bug — an unminified/dev-mode dependency can ship silently, with the app still functioning (StrictMode's dev-only checks don't crash anything, they just run extra work), so the only way this was ever going to surface was directly measuring bundle composition/size or deliberately counting network requests, neither of which any prior round's testing happened to do.
- **Persistent, SQLite-backed story cache + non-blocking startup warmup (changed v1.0z, SAC-073):** The in-memory `storyCache`/`questionsCache` `Map`s (v1.0r) sped up repeat loads *within* one running server process, but every fresh deploy/restart started from zero — the first user to request any given scenario after a deploy always paid the full ~20s generation cost, no matter how many times that exact scenario had already been generated in a prior run. `getCachedStory`/`cacheStory` now read/write a `story_cache` table in the same SQLite file the usage-tracking log already uses (`./data/api_usage.db`, `node:sqlite`'s `DatabaseSync` — no new dependency), keyed on `scenario` as the PRIMARY KEY so a regenerate's `INSERT ... ON CONFLICT DO UPDATE` replaces the row in place rather than accumulating duplicates. Both cache functions are wrapped in try/catch that degrades to "treat as a miss, generate fresh" on any read/write failure (a locked or corrupt db file should never turn into a hard failure for the user). New `warmupCache()` walks all 8 real scenario titles (`WARMUP_SCENARIOS`, deliberately duplicated from — and must be kept in sync with — `ScenarioSelector.jsx`'s `DEFAULT_SCENARIOS`, since there's no shared module between the frontend bundle and this backend to import a single source from) and pre-generates any that aren't already cached, sequentially with a 500ms gap between calls (deliberately not parallel, to avoid firing 8 concurrent Claude calls against one API key on every deploy). **Deliberately fired *after* `app.listen()`, not awaited before it** — see Decisions Log for why the prompt's own literal spec (`await warmupCache()` before `listen()`) was corrected before building: it would have delayed the server actually binding its port by several minutes on every single deploy, during which every endpoint (not just story loading) would be unreachable, risking the same class of incident as the SAC-062 Node-18/`node:sqlite` crash-loop. `/health` now also reports `cacheReady` (bool) and `cacheWarmup` (`{total, alreadyCached, generated, failed}`) so warmup progress is observable without reading server logs — the top-level `status` field deliberately stays `'ok'` throughout warmup rather than switching to something like `'warming-up'`, since the server genuinely is fully operational the whole time (a cache-miss during warmup just falls through to a normal live generation, exactly like any scenario outside the warmup list). Verified live: server bound its port and answered both `/health` and a real `/api/translate` call in under a second while warmup was still running in the background; all 8 scenarios warmed successfully in ~2.5 minutes with zero failures; a cache hit after warmup completed in ~100ms; regenerating an already-cached scenario correctly updated its existing row (same total row count, new `created_at`) rather than duplicating it. **Checked, not assumed, whether the cache actually survives a real Railway redeploy** (as opposed to a same-container restart): `railway volume list` showed no persistent volume attached, and a real back-to-back redeploy test (deploy → confirm 8/8 warm → redeploy the identical code → check again) showed the second deploy start from `alreadyCached: 0` — the SQLite file does not survive an actual redeploy on this platform, same as this project's other ephemeral-filesystem caches. The auto-warmup is what actually delivers the "fast from day one of a deployment cycle" goal in practice, not file persistence across deploys — see the corrected Tech Stack table entry above.
- **Custom Listening Topics: a pre-generated story handed to `ListeningStoryView` directly, bypassing its own fetch (added v1.1a, SAC-071):** New `CustomTopicForm.jsx` (a modal, same `isOpen`/backdrop-click/ESC shell as `RegenerateModal.jsx`) collects a topic (100 char max) and a Beginner/Intermediate/Advanced difficulty, calls the new `POST /api/generate-custom-story`, and hands the resulting `{sentences, vocabulary}` straight to the parent rather than a scenario name — `ScenarioSelector.jsx` gained a `showCustomTopic` prop (only `true` for Listening Mode, since this component is also shared with the currently-disabled Conversation Mode and a custom Listening topic doesn't belong there) that inserts a "✨ Create Custom Topic" card at position 3 of the scenario grid and owns the modal's open/close state; `App.jsx` gained `customSession` state (`{storyData, topic, difficulty, nonce}`) that flows into `<ListeningStoryView>` as new `storyData`/`customDifficulty` props alongside the existing `scenario` prop — `scenario` doubles as the topic's own text for a custom session, which turns out to need zero special-casing anywhere else in the component (header text, `sessionStorage` play-pulse key, `RegenerateModal`'s displayed topic, the `/api/story-questions` cache key, and `SessionReview`'s saved-history display all already just interpolate whatever string `scenario` holds). `key={customSession ? \`custom-${customSession.nonce}\` : scenario}` guards against the edge case of a user typing the *exact same* topic text twice in a row through the form, which `key={scenario}` alone wouldn't force a remount for. Every handler that already cleared `scenario` (`handleReset`, `handleBackToModes`, `handleSelectScenario`, `handleApiError`, `handleSelectMode`) was extended to also clear `customSession`, specifically to prevent a completed custom story's `storyData` from leaking into a subsequently-picked pre-built scenario — verified live this doesn't happen (created a custom story, went back, picked a pre-built scenario, confirmed its header showed the real scenario name, not the leftover custom topic, and that it loaded from the SQLite cache instead of calling the custom endpoint). `loadStory()` in `ListeningStoryView.jsx` gained a three-way branch decided once per call: an initial custom load reads `storyDataRef.current` directly (no fetch — CustomTopicForm already generated it); a custom *regenerate* (`regenerate === true` while `isCustomRef.current` is `true`) calls `/api/generate-custom-story` again with the original topic+difficulty instead of the pre-built `/api/generate-story` endpoint entirely; everything else is the unchanged pre-built path. All three converge on the exact same SAC-074 background-questions-fetch logic afterward, so that decoupling applies to custom topics too without having been touched. `/api/generate-custom-story` unconditionally clears any `questionsCache` entry for that topic text before generating — every call to this endpoint (first load or regenerate) produces a brand-new, never-cached story, so a prior generation's cached MCQ data for the identical topic text would otherwise go stale, the same class of bug SAC-034 fixed for pre-built regenerate. Custom-story API cost is tracked under its own `'Custom Stories'` bucket in `/api/usage-stats` (verified live in the breakdown) rather than folded into `'Stories'`, since it's a genuinely distinct cost driver a user might want visibility into separately.
- **Fixed: a scheduled-but-not-yet-fired speak() could survive unmount and play anyway (fixed v1.1b, SAC-075):** `speakSentenceAt`, `resumeSentenceFromBoundary`, `speakClaritySegment`, and `playTranscriptSentence` all cancel any in-flight utterance, then delay the actual `.speak()` call by `SPEAK_START_DELAY_MS` (175ms — the v1.0m-era fix for clipped opening syllables), gated by `if (token !== utteranceTokenRef.current) return`. The mount effect's cleanup already called `synthRef.current.cancel()` on unmount (stopping anything *currently* playing) and cleared `gapTimeoutRef` (the inter-sentence auto-advance timer), but never touched `utteranceTokenRef` — so a `setTimeout` scheduled just before unmount (e.g. the instant after clicking Play, or Next/Prev, or a speed change, or a transcript sentence icon) would still fire ~175ms later with its token still valid, calling `synthRef.current.speak()` on a component that no longer existed. `synthRef.current` itself is just `window.speechSynthesis`, a browser global that doesn't care whether the React component holding the ref is still mounted, so the call succeeded and started genuinely un-stoppable audio — there was no more mounted component, no button, nothing left to cancel it. Fix: the cleanup now also does `utteranceTokenRef.current++`, which invalidates every pending guard check regardless of which of the four call sites scheduled it — the exact same "supersede via token bump" mechanism this file already uses everywhere else (see the v1.0h token-guard entry below), just extended to cover the unmount case too. **Confirmed the actual race, not just the theory**, before writing the fix: scripted a real browser to click Play then immediately navigate away (~8ms later, well inside the 175ms window), then polled `speechSynthesis.speaking` continuously (not just once) for a full second afterward — it went `true` at the ~200ms mark and stayed `true` for the rest of the poll window, reproducing the reported bug exactly on the unpatched code. The same script against the one-line fix showed no phantom speech at all across the same window. Also re-verified normal playback, mid-scenario-switch, pause-then-navigate, and rapid multi-navigation all still behave correctly afterward, so the fix doesn't just close the race, it doesn't regress anything else that shares the same token.
- **UI/UX polish pass — header, story header, Quick Translate, footer controls (changed v1.1c, SAC-077):** Six changes, most self-contained. (1) `App.jsx`'s header changed from title+version-badge side-by-side to stacked (title on its own line, `v1.1c` below it in `text-xs text-ink-faint`, still a clickable `AboutModal` trigger). (2) `ScenarioSelector.jsx`'s "← Change Mode" link removed outright (not just hidden) — `App.jsx` no longer passes `onBackToModes` to it, and the now-dead prop/JSX block was deleted rather than left as unreachable code, matching this project's established "remove dead code, don't leave it disabled" pattern (same as `ListeningHeader.jsx`'s removal). (3) `ListeningStoryView.jsx`'s story header dropped the "— Listen carefully" suffix and changed from `text-small text-ink-muted` to `font-bold text-ink text-heading-2` (18px/700, matching the exact class combination `ScenarioSelector.jsx`'s own card titles already use) plus a `truncate` class — verified live with a genuinely long custom topic ("A really long and detailed topic about planning an elaborate surprise birthday party") that it single-line-ellipsizes rather than wrapping or overflowing (`scrollWidth` 710px vs. `clientWidth` 294px at a 390px mobile viewport, confirmed truncated). (4) The Quick Translate button lost its teal pill background and "⊕" icon, now plain `text-small text-ink-muted` text matching the visual weight of the adjacent Display Spanish/English checkboxes. (5) Speed, Clarity, and the Regenerate icon (previously its own separate row above the fixed footer) are now one row: font bumped from `text-xs` (12px) to `text-base` (16px, ~+33%, closer to the requested "+30%" than the initially-tried `text-sm`/14px) and color darkened from `text-ink-faint` to `text-ink-muted`; verified no horizontal overflow at a 390px mobile viewport (`scrollWidth === clientWidth`) via both a computed-style check and a visual screenshot. (6) **The one item the prompt explicitly asked to have clarified rather than guessed** ("vocabulary hover: full coverage or vocab-only?") — investigated `HoverableText.jsx` directly rather than asking blind: a word gets its dotted underline/click-to-define only if it's a key in the `vocabulary` map passed in, and pre-built stories' `/api/generate-story` prompt already asks for *every* distinct word, while `/api/generate-custom-story` (built last round, SAC-071) asks for only "8-12 useful words" — a real, concrete inconsistency this session introduced when building custom topics, not a regression in older code, and not actually ambiguous once traced. Fixed by giving `generateCustomStoryFromClaude` the identical "every distinct word" instruction `/api/generate-story` already uses, and raising its `max_tokens` from 3000 to 10000 to match (a 250-300 word custom story's *full* per-word vocabulary list runs proportionally larger than a 100-150 word pre-built story's already-documented 50-90 entries). Verified live: generated a real custom story, captured its actual API response, and directly diffed every distinct word in the story text against the returned vocabulary list — 59 distinct words, 59 vocabulary entries, 0 missing, 100% coverage (generation still completed in ~16s despite the larger budget).

- **Difficulty now applies to every story, not just custom topics; `story_cache`/`questionsCache` both keyed on `(scenario, difficulty)` (changed v1.1d, SAC-076):** Before this round, difficulty was entirely a custom-topic concept (SAC-071) — `/api/generate-story` (pre-built scenarios) had no `difficulty` parameter at all and always generated at one fixed, implicitly-beginner level, despite the prompt's claim that it "already accepts difficulty from SAC-071." A new module-scope `DIFFICULTY_LEVELS`/`DIFFICULTY_GUIDE` in `server.js` is now shared by both `generateStoryFromClaude` (pre-built) and `generateCustomStoryFromClaude` (custom, which previously had its own separate, now-removed local `difficultyGuide` object) — `generateStoryFromClaude` gained a `difficulty = 'Beginner'` parameter, and its prompt's hardcoded "for a beginner learner... simple grammar" phrasing was replaced with a `Difficulty level: ${difficulty}. ${DIFFICULTY_GUIDE[difficulty]}` line. The story's sentence-count/length constraints (100-150 words, 7-10 sentences, 10-15 words/sentence — carefully tuned across many earlier rounds for TTS/comprehension pacing) were deliberately left unchanged across all three difficulty levels; only vocabulary/grammar complexity varies. `story_cache`'s SQLite primary key changed from `scenario` alone to `(scenario, difficulty)`, since a single scenario can now hold up to 3 independently-cached story versions simultaneously — `getCachedStory`/`cacheStory` both take a `difficulty` parameter now. The in-memory `questionsCache` needed the identical treatment for a reason the prompt itself didn't call out: without a difficulty-aware cache key, switching a scenario's difficulty (e.g. Beginner → Advanced) could serve `/api/story-questions` data that was actually written for a *different* difficulty's story text — a real content-mismatch bug, not a hypothetical one, caught by reasoning through the existing single-key `Map` before building rather than after. Added a `questionsCacheKey(scenario, difficulty)` helper and threaded `difficulty` through `getCachedQuestions`/`cacheQuestions` and both call sites that touch this cache (`/api/generate-story`'s regenerate-invalidation, `/api/generate-custom-story`'s unconditional invalidation). `/api/story-questions`'s own prompt text (previously hardcoded `"Here is a beginner-level Spanish story..."` regardless of the story's actual difficulty) now interpolates the real difficulty, since telling Claude an Advanced story is "beginner-level" while asking it to write comprehension questions could plausibly skew question wording/complexity to not match the story it's actually testing. **Warmup scope, a real cost/time tradeoff surfaced and resolved via `AskUserQuestion` rather than assumed:** warming all 3 difficulty levels for all 8 scenarios on every startup would have roughly tripled both the ~2.5min warmup time and the real Claude API cost of every single deploy; Vinay chose to keep warming Beginner only (unchanged from SAC-073), leaving Intermediate/Advanced to generate on-demand the first time a user actually requests them for a given scenario (~20s that one time, then cached for the rest of that deployed instance's life, same ephemeral-on-redeploy caveat already documented for the SAC-073 cache). Frontend: `ListeningStoryView.jsx`'s previously-frozen `customDifficultyRef` (captured once at mount, SAC-071) generalized into a mutable `difficultyRef` usable by both pre-built and custom sessions — `RegenerateModal.jsx` gained a difficulty radio-group (defaulting to the story's current difficulty, re-synced every time the modal reopens) that reports the user's selection back through `onConfirm(selectedDifficulty)`; `handleConfirmRegenerate` updates `difficultyRef.current` before calling the existing `handleRegenerateStory()`, and all three fetch call sites in `loadStory()` (`/api/generate-story`, `/api/generate-custom-story`, `/api/story-questions`) now pass `difficultyRef.current`. **A genuinely new local-dev-only migration issue surfaced and fixed, not present in the original plan:** unlike Railway's ephemeral filesystem (where a fresh `CREATE TABLE` with a new key shape is free on every redeploy, confirmed in the SAC-073 round), the local dev SQLite file (`./data/api_usage.db`) *does* persist across `npm run backend` restarts on this machine — restarting against a pre-existing dev database with the old scenario-only-PK `story_cache` table threw `SQLite error: no such column: difficulty` on every warmup attempt. Fixed with a one-time startup check (`PRAGMA table_info(story_cache)`; if the table exists without a `difficulty` column, `DROP TABLE` before the `CREATE TABLE IF NOT EXISTS` runs) — harmless, since losing a locally-warmed cache just means it re-warms on next startup, same as any fresh deploy. **Verified live** (real Chrome against local dev servers, via Puppeteer): a pre-built scenario's Regenerate modal correctly shows all 3 difficulty options defaulting to Beginner; regenerating at Advanced produced genuinely different, more complex content (verified by diffing sentence 1's actual text, not just trusting the prompt change — Beginner: "Diego es piloto y llega muy cansado a un hotel pequeño"; Advanced: "Valeria, una arquitecta chilena, llegó al hotel boutique pasada la medianoche"); reopening the modal afterward correctly defaulted to Advanced (confirming `difficultyRef` updates persist across the session, not just a one-shot); a genuinely fresh (non-regenerate) load of an already-warmed scenario hit the SQLite cache in ~70ms; direct `curl` timing on a from-scratch Advanced custom-topic generation (35s) confirmed the subjunctive-heavy, idiom-rich output the `DIFFICULTY_GUIDE` text calls for ("arruine," "pudiera," "acabaran" — all subjunctive forms absent from Beginner output); and a full custom-topic UI regenerate-at-Advanced flow (not just the direct API call) also produced correctly-differentiated content with zero console errors. One real bug caught by this testing and fixed before shipping: the `/api/story-questions` handler's `cacheQuestions(scenario, questionsData)` call site was missed in the initial pass of the `getCachedQuestions`/`cacheQuestions` signature change — it kept calling the old 2-argument form, which silently shifted `questionsData` into the `difficulty` parameter and logged `[object Object]` instead of a real difficulty string; caught via that exact log line during live testing, not by code review, and fixed by updating the call to `cacheQuestions(scenario, difficulty, questionsData)`. **Also confirmed, not just assumed, that Regenerate has always bypassed the SQLite cache entirely** (pre-existing behavior, unchanged by this round) — clicking Generate in the modal always calls with `regenerate: true`, which the route handler treats as "always generate fresh, never serve cached," even when switching back to a difficulty that was already generated earlier in the same session. This is why re-selecting Beginner after regenerating at Advanced produced a *different* Beginner story (different random names/details) than the original, not a bug — an initial test assumption that this should be a fast cache-hit was itself wrong, corrected after actually reading `/api/generate-story`'s route logic rather than assumed from the feature's description.

- **Play button pulse: finite 3-iteration CSS animation, not `infinite`; re-triggerable on Regenerate (changed v1.1e, SAC-078; animation style refined v1.1f):** The SAC-048/v1.0u pulse (`.play-button-pulse { animation: playButtonPulse 1.2s ease-in-out infinite; }` — note this original 1.2s is coincidentally the same number the v1.1f refinement below lands back on for a different reason, unrelated) never had a built-in stop — it relied entirely on `isFirstLoad` flipping to `false` (on a real Play click, or already suppressed outright for Regenerate) to remove the class, meaning a story loaded but never clicked kept visibly pulsing forever. v1.1e changed the CSS itself to a finite `3`-iteration count, `forwards` fill-mode — making the finite stop a CSS-native guarantee rather than something JS has to enforce correctly every time. v1.1e's first pass kept the original `scale(1.5)` bounce, just capped (`playButtonPulse 0.6s ease-in-out 3 forwards`, 1.8s total); a same-day follow-up round (v1.1f) replaced the bounce entirely with a gentler `opacity`/`color` breathing fade at a slower 1.2s-per-pulse pace (3.6s total) — that follow-up prompt's own CSS snippet named a keyframe (`play-pulse`) and CSS variables (`--text-primary`/`--text-muted`) that don't exist anywhere in this codebase; kept the real `playButtonPulse` name and substituted this project's actual tokens (`:root` in `index.css` defines `--color-text`/`--color-text-muted`, not `--text-*`), but didn't use `--color-text-muted` either — that token's a body-text gray meant for text on the light `--color-surface` background, and would read as a mismatched dulling against this specific button's solid `bg-primary` teal circle + white icon styling. Used `--color-primary-light` (already in the palette) as the fade's low point instead — a light teal that stays visually coherent against the darker teal circle. The JS-side redundant safety-net timeout (`pulseTimeoutRef`, in `ListeningStoryView.jsx`) has to track whatever the CSS's actual total duration is — updated from 1800ms to 3600ms alongside the CSS change, since a mismatch there would have the JS side quietly flipping `pulseAnimationActive` back to `false` while the CSS animation was still visibly mid-fade. `isFirstLoad` (a single persistent boolean) was replaced with two separate concerns: `shouldPulseRef` (computed fresh at the top of every `loadStory()` call as `regenerate || !hasAlreadyPlayed(scenario)` — eligibility, decided once per story-load) and a new `pulseAnimationActive` state (the actual visual on/off switch for the CSS class, toggled by a `useEffect` keyed on the `story` object itself, which is a fresh reference on every load *and* every regenerate). Turning the class off then back on needs an actual paint tick in between to make the browser replay the CSS animation — a same-tick `false` then `true` doesn't restart it — so the effect goes through `requestAnimationFrame` rather than setting `true` directly; a `setTimeout` (3600ms as of v1.1f, was 1800ms under v1.1e's faster scale-bounce timing — cleared+re-armed on every new trigger) then flips it back to `false` as a redundant safety net (the CSS's own 3-iteration count is what actually stops it; the timeout mainly exists so `handlePlayPause`'s explicit early-stop-on-click has a live piece of JS state to clear instead of waiting out the animation). **Deliberately reverses a prior documented decision, not an oversight:** SAC-046-048's v1.0u round explicitly suppressed the pulse after Regenerate ("by this point the user has already found and used the controls at least once") — this round's prompt explicitly listed "Regenerate/new story → pulses 3x again" as desired behavior, so that suppression (`setIsFirstLoad(false)` in `handleRegenerateStory`) was removed; `shouldPulseRef`'s `regenerate ||` clause is what makes every regenerate pulse-eligible regardless of whether the scenario was already marked played. **Verified live (v1.1e, scale-bounce version):** a fresh never-played scenario pulses immediately and the CSS class is confirmed gone by ~2s (computed `getComputedStyle` showed `animation-duration: 0.6s`, `animation-iteration-count: 3`, `animation-fill-mode: forwards` — the finite stop is real, not just visually appearing to stop); clicking Play mid-pulse removes the class within ~150ms rather than waiting out the remaining animation time; Regenerating an already-played scenario re-triggers a full fresh pulse (confirming the `requestAnimationFrame` off-then-on toggle actually restarts the CSS animation on a component that never remounts); revisiting an already-*actually-played* (Play was clicked, not just loaded) scenario without regenerating shows no pulse at all. **Re-verified live (v1.1f, opacity/color-fade version):** `getComputedStyle` on the class now shows `animation-duration: 1.2s`, `animation-iteration-count: 3`, `animation-fill-mode: forwards` (3.6s total, matching the updated 3600ms JS timeout); pulse confirmed still active at the 2s mid-point and confirmed stopped by 4s; a screenshot taken mid-pulse shows the button visibly dimmed/lighter-teal at its fade's low point, confirming the opacity/color animation actually renders rather than silently no-op'ing (a real risk given the `color` keyframe targets a Unicode glyph — ▶/⏸ — whose color response to CSS varies by platform font rendering; the `opacity` component was written to carry the visible effect on its own regardless of that uncertainty, and the screenshot confirms it does).

- **Grammar Breakdown Icons: per-sentence ⓘ, dynamically generated in the background (added v1.2a, SAC-079):** New `/api/generate-sentence-explanations` endpoint + `generateSentenceExplanations(sentences, difficulty)` in `server.js` — given an array of Spanish sentence strings and a difficulty, returns one `{ sentenceIndex, phrase, literalTranslation, englishSyntax, pattern }` object per sentence via a single Claude call (0-indexed consistently in both the numbered list shown to Claude and the requested `sentenceIndex` field, deliberately — the originally-pitched version of this prompt's given code showed Claude a 1-indexed list while asking for a 0-indexed `sentenceIndex` back, a real ambiguity risk that could have led Claude to echo 1-indexed values instead). `max_tokens: 3000` (this project has hit truncation before on structured-JSON endpoints at tighter budgets — v1.0d, SAC-071 — so erred generous for up to 10 sentences × 4 short fields). Response parsing uses its own `\[[\s\S]*\]` regex rather than the existing `extractJson()` helper, since that helper only matches a `{...}` object and this endpoint's top-level response is a `[...]` array. Cost tracked under its own new `'Explanations'` bucket in `featureForEndpoint()`, matching the established precedent of giving a genuinely distinct cost driver its own visibility (same reasoning as Custom Stories' own bucket). **Deliberately uncached** — not asked for this round, and a disclosed real cost tradeoff: a warmed Beginner scenario replayed by many different users re-generates explanations fresh every single time, unlike `story_cache`/`questionsCache`. Parked as a natural follow-up idea in PENDING.md rather than silently built this round. Frontend: `ListeningStoryView.jsx` fires this in the background via a `useEffect` keyed on the same `story` object the SAC-078 pulse effect already watches (so it re-fires on every load *and* every regenerate, for the same "fresh object reference" reason), using `difficultyRef.current` (not `story.difficulty`, which doesn't exist — the story response from `/api/generate-story`/`/api/generate-custom-story` has never included a `difficulty` field, difficulty is a request parameter only; the originally-pitched frontend code assumed `story.difficulty` existed, which would have silently fallen back to `'Beginner'` every time regardless of the story's actual difficulty). `sentenceExplanations` is keyed by sentence index as an object map (not a plain array matching response order) — deliberately, since Claude returning entries out of order or with a gap (skipping a "trivial" sentence despite being asked not to) would otherwise silently misalign every explanation after the gap; each entry is placed at its own `sentenceIndex` key regardless of array position. Reset to `{}` at the top of the effect on every new `story`, and guarded with the same `isStale()`-style pattern `loadStory()`'s own background `/api/story-questions` fetch uses, so a slower-to-resolve explanations request for an older story can't land after a newer regenerate and overwrite its (still loading) explanations with stale ones. `ExplanationIcon.jsx` exports two presentational components rather than one self-contained button+panel unit — `ExplanationIcon` (controlled: `explanation`/`isOpen`/`onClick` props, renders as a disabled ⓘ while `!explanation`, becomes clickable once its specific sentence's explanation arrives — deliberately never renders `null` while loading, so the icon doesn't pop into existence later, only its clickability changes) and `ExplanationPanel` (pure render of the four fields). Splitting them was necessary, not just a style preference: the panel has to appear as a block-level sibling *below* the icon row (matching this file's existing `openTranslationIdx`/🌐-toggle panel placement exactly), and a Fragment returned from inside a `flex`/inline icon row would instead render the panel as a squeezed flex/inline child in the wrong position — so open/closed state (`showCurrentExplanation` for the single-sentence Display Spanish block; `openExplanationIdx`, the same single-nullable-index pattern as `openTranslationIdx`, for the per-sentence Transcript list) lives in `ListeningStoryView.jsx` and is passed down, not owned internally by the icon component. **Corrected before building, not copied as-given:** the originally-pitched code called `logApiCall('generate-sentence-explanations', duration)` — this project's real `logApiCall` signature is `(endpoint, model, inputTokens, outputTokens)`, an entirely different shape (a duration in ms where a model name was expected, no actual token counts logged at all) — fixed to log the real `message.usage.input_tokens`/`output_tokens` from the Claude response, matching every other endpoint in this file. The given `ExplanationIcon.jsx` also used inline `style={{}}` objects against CSS custom properties (`--text-primary`, `--text-muted`, `--surface-1`, `--border`, `--radius`) that don't exist anywhere in this codebase's actual `:root` token set (`src/index.css` defines `--color-text`/`--color-text-muted`/etc.) — the same class of mismatch this project has now hit and corrected three separate times (SAC-071, the SAC-078 refinement, and this round), strongly suggesting external prompt drafts for this project are being written without checking the actual design-token names first; rewrote using this codebase's real Tailwind utility classes (`text-ink`, `text-ink-muted`, `bg-primary-light`, `rounded-control`, etc.) matching every other component. **Verified live:** direct `curl` to the new endpoint with a real 8-sentence story returned correct, well-formed explanations for all 8 sentences with accurate 0-indexed `sentenceIndex` values and genuinely correct/useful Spanish-grammar content, no truncation, in ~12s; in the browser, the Display Spanish block's ⓘ icon starts visibly disabled ("Loading explanation…"), becomes enabled ~12-13s later once the background fetch resolves, opens to show real phrase/literal-translation/English-syntax/pattern content matching the direct API test, and closes on a second click; regenerating a story immediately re-disables the icon (confirming the stale-clearing reset) and a fresh, genuinely different batch of explanations arrives ~13s later for the new story's different sentences; zero console errors throughout. **Disclosed testing limitation:** could not verify the Transcript section's ⓘ icons interactively in this session, since reaching them requires `playStatus === 'finished'`, and this specific session's headless Chrome instances returned 0 voices from `speechSynthesis.getVoices()` (confirmed via direct check) — a pre-existing, unrelated environment issue (most likely tied to a multi-hour system idle/sleep gap that occurred mid-session, before any of this round's testing began) that also blocked a plain, code-unrelated "click Play on sentence 1" sanity check from ever completing. Not a SAC-079 regression. The Transcript integration uses the identical `ExplanationIcon`/`ExplanationPanel` components and the identical `sentenceExplanations` state as the already-verified Display Spanish integration (just `idx` instead of `currentIndex` as the lookup key), so there's no functionally distinct new code path left unexercised there — only the pre-existing `showTranscript` toggle mechanism itself, already verified working in multiple earlier rounds this session, remains between "story finished" and these icons becoming visible.

## Shipped in v1.0q — Phase 2.7 Audio & UX Polish (Prompts #013–#017)

Five rounds (#013–#017) accumulated locally, each held at the commit/deploy gate pending a human speech-clarity listen-test, per Prompt #013's original instruction. Vinay confirmed locally and explicitly requested the deploy; all five rounds shipped together in one commit (`ffb990b`) and one production deploy — Railway backend + Netlify frontend both live at v1.0q, verified via a real Playwright check against the production URLs (badge visible, scenario picker loads, zero console errors).

- **Explicit Spanish voice selection + slower default rate (added, Prompt #013 Critical Fix):** New `src/speechUtils.js` exports `applySpanishVoice(utterance)` — sets `lang='es-ES'`, then explicitly assigns `utterance.voice` to the first available `es-ES` (or any `es-*`) entry from `speechSynthesis.getVoices()`, wrapped in try/catch (degrades to lang-only on any failure, never crashes playback). Applied at all 4 utterance-creation sites across `ListeningStoryView.jsx` and `ConversationView.jsx`. Listening Mode's default rate lowered 0.5x → 0.3x. Conversation Mode's separate rate system (0.8x default) deliberately untouched — out of this prompt's stated scope. **Word-level pause fallback (the prompt's "Fix 3") was explicitly NOT built** — it's conditional on whether the first two fixes are enough, which only a human ear can judge; building it speculatively would be guessing at an unconfirmed problem.
- **Speed labels changed again: Normal/Slow/Slower = 0.3/0.2/0.15x (changed, Prompt #013 Task 1):** Supersedes v1.0m's `Slow/Normal/Fast` = `0.6/0.5/0.4x` (shipped one round ago, in Prompt #012) — that round's speeds were judged still too fast for word clarity. No "fast" tier exists anymore; all three options are slow-for-comprehension tiers.
- **SAC-031: Sentence navigation redesign, single-sentence "stop after playing" mode (added, Prompt #013 Task 2):** `⏮ Restart-whole-story` and the numbered 1-9 jump markers (SAC-026, v1.0m) replaced by 5 controls: `🔄 Replay` (restarts *just the current sentence* — a genuinely new concept, not what the old ⏮ did), `⏮ Prev`, `▶‑⏸` (unchanged global auto-advancing play/pause), `⏭ Next`, `⏩ End`. All four single-sentence actions route through a new `manualNavigateTo(idx)` helper that sets a `manualNavRef` flag; `handleSentenceUtteranceEnd` checks that flag and, when set, stops after the sentence instead of gap-then-auto-advancing — this is what makes Prev/Next/Replay/End "play one sentence and stop" rather than continuing the story. The flag is only cleared by the *global* Play/Pause button's idle-branch, which is also what re-enables normal auto-advance. Real bug caught and fixed while building this: that idle-branch previously always called `speakSentenceAt(0)` unconditionally — harmless before this feature (since 'idle' was only ever reached at initial mount), but with manual nav now able to leave the view in 'idle' mid-story, pressing global Play after a manual nav would have silently restarted the whole story from sentence 1. Fixed to resume from `indexRef.current`. Also: if manual navigation reaches the actual last sentence, the completion handler sets `'finished'` (not `'idle'`) specifically so the Comprehension Check stays reachable that way too, not only via letting the full story auto-play to the end.
- **SAC-032: Nav buttons moved from in-view headers to a global sticky FooterNav (added, Prompt #013 Task 3):** `ListeningHeader.jsx` no longer renders Back/Change Mode/Diff-Scenario — it's now just the home for the "Regenerate Story" button (renders nothing if no `onRegenerate` handler is passed). `ConversationView.jsx`'s equivalent inline nav row removed the same way. New `FooterNav.jsx`: `position: fixed` bottom bar, 4 buttons (Back/Mode/Topics/History), rendered once in `App.jsx` so it's present on every screen (mode selector, history dashboard, session views alike) — `App.jsx` gained `pb-20` so the fixed footer doesn't cover the last bit of scrollable content. **The hard part was preserving save-on-exit:** both views used to have their own in-view Back button wired to view-specific save-then-navigate logic; removing that button meant the new *external* FooterNav needed a way to still trigger it. Solved with `forwardRef`/`useImperativeHandle` on both `ListeningStoryView` and `ConversationView` (exposing `{ back: <their save-then-navigate function> }`), and a single shared `activeViewRef` in `App.jsx` (only one view is ever mounted at a time, so one ref covers both — React nulls it automatically on unmount). `FooterNav`'s handlers call `activeViewRef.current?.back?.()` first when a session is active, before falling back to generic navigation. Verified this genuinely works (not just "doesn't throw"): completed a session, used FooterNav Back, checked History — the session was actually there.

**Decisions made without an explicit spec, Prompt #013:** (1) A test-mock bug, not an app bug, caught mid-round: an early test fabricated plain-object fake Spanish voices for `getVoices()` to return; Chromium's real `utterance.voice` setter rejected them (real `SpeechSynthesisVoice` has no public constructor a mock can imitate), which looked like a real error until checking what this environment's actual, *unmocked* `getVoices()` returns — genuinely zero voices, confirming the app's own graceful-fallback path was already handling this correctly and the test mock just needed to match reality. (2) A `fullPage: true` screenshot of the new footer showed it duplicated mid-page — verified as a known Playwright `position: fixed` + full-page-stitching artifact (not a real bug) via a plain viewport screenshot, which showed correct single-instance bottom placement. (3) **Genuinely cannot verify the one thing this whole prompt was about:** whether the speech actually sounds clearer. This isn't a "real device vs. emulated" gap like every other disclosed limitation this project has hit — there is no audio output/perception capability here at all, full stop. Built the fixes faithfully, verified everything checkable in code, and stopped at the commit/deploy gate exactly as instructed, pending Vinay's own ears.

### Prompt #014 additions (target v1.0n — never its own release, shipped as part of v1.0q)

- **Speed buttons reverted to raw multipliers, default back to 0.8x (changed, Prompt #014 Task 1):** `x1.0/x0.8/x0.5` replaces Prompt #013's `Normal/Slow/Slower` (0.3/0.2/0.15x) — this round's own framing is the confirmation loop closing: the complaint changed from "slurred" (Prompt #013's problem) to "too slow" (this round's problem), meaning the voice-selection + rate fix from #013 actually worked and only the specific numbers needed adjusting back up. `ConversationView.jsx`'s separate Repeat-button rates got the same label treatment for consistency (0.6x → 0.5x on its slowest tier).
- **Comprehension toggle now also gates Vocabulary Matching (changed, Prompt #014 Task 2):** `showMCQ` renamed `showComprehension`; the render condition for `<VocabularyMatching>` gained `&& showComprehension` (previously it rendered unconditionally once `playStatus === 'finished'`, no toggle at all). `Display Transcript` remains a fully independent toggle.
- **Vocabulary Matching: pills instead of stacked buttons, word audio icon, manual-advance instead of a timer (rewritten, Prompt #014 Tasks 3-5):** Answer options are now small `rounded-full` pills in a `flex flex-wrap` row (explicit spec-driven exception to the 44px tap-target convention, same class as SAC-026's 32px sentence markers). New `playWord(text)` plays just the current word via a fresh utterance (`applySpanishVoice` + a `rate` prop now threaded down from `ListeningStoryView.jsx`, defaulting to 0.8 for safety). The `ADVANCE_DELAY_MS` auto-advance timer is gone entirely — a correct match now shows "✓ Correct!" + examples + a persistent "Next →" button, staying visible until the user clicks it.
- **Story title compacted into the player-controls card (changed, Prompt #014 Task 6):** The standalone `border-l-4 border-primary bg-primary-light` title block is gone; a plain muted subtitle line now sits at the top of the same card as the Replay/Prev/Play/Next/End controls.
- **Three real timer/race-condition bugs found via testing and fixed (added, Prompt #014):**
  1. *Stale autoplay-check clobbers a legitimately-finished manual-nav session* — SAC-024's 900ms post-load check (`!speaking && !gapTimeoutRef.current`) is also incidentally true once a manually-navigated session reaches a genuine 'finished' state well within that window, so it would wrongly fire `setAutoplayFailed(true)` + `setPlayStatus('idle')` and yank the just-shown Comprehension/Vocab UI back out. Fixed: bail if `manualNavRef.current` is true.
  2. *The 300ms initial-autoplay-kickoff has the same bug in reverse* — a very fast manual-nav click within that window gets silently overwritten back to sentence 0 once the pending kickoff fires. Fixed the same way.
  3. *`VocabularyMatching.jsx`'s wrong-guess auto-clear timer could survive into a subsequent correct match* — `selectOption`'s correct branch never cancelled `retryTimeoutRef` from an earlier wrong guess, so it could fire ~900-1200ms later and wipe the persistent "✓ Correct!" state (and Next button) out from under an already-successful match. This one was hard to isolate — the visible symptom ("Next button not found") was several layers removed from the cause, and required polling DOM state every 250ms across the relevant window to catch the exact disappearance point. Fixed: `selectOption` now unconditionally clears `retryTimeoutRef` before branching.
  All three share a lesson worth remembering for any future timer-based logic here: **a delayed callback needs to re-check whether the state it assumed still holds by the time it actually fires**, not just whether the component is still mounted.

**Decisions made without an explicit spec, Prompt #014:** Same as Prompt #013's point (3) — speech clarity still cannot be verified in this environment; still stopped at the commit/deploy gate pending Vinay's confirmation, this time also covering everything built in #014 on top of #013.

### Prompt #015 additions (v1.0m → v1.0o)

- **Syllable-loss fix: 75ms delay between cancel() and speak() (CRITICAL fix, Prompt #015 Task 1):** Root cause of the reported bug ("María va al restaurante" playing as "ía va al restaurante" on Prev/Next/Replay/End, the transcript 🔊 icon, and the vocab word 🔊 icon) — `speechSynthesis.speak()` was firing immediately after `.cancel()`, before the engine had finished tearing down the previous utterance's audio pipeline, clipping the start of the new one. New shared `SPEAK_START_DELAY_MS = 75` constant in `speechUtils.js`; every real call site now does `cancel()` → `setTimeout(75ms)` → `speak()`, guarded by a token check inside the timeout (existing `utteranceTokenRef` in `ListeningStoryView.jsx`; new `speakTokenRef` added to `ConversationView.jsx` and `VocabularyMatching.jsx`) so a delayed `speak()` becomes a no-op if something newer already superseded it during the wait, rather than speaking a stale/cancelled utterance late. `HoverableText.jsx` was checked (named in the prompt's file list) but has no audio call site — word-click there just shows a text tooltip.
- **Vocab word auto-plays on correct match (added, Prompt #015 Task 2):** `VocabularyMatching.jsx`'s `selectOption` now calls `playWord(currentWord.word)` ~200ms after a correct match (staggered so it doesn't overlap the correct-match beep), reusing the same `playWord` the 🔊 icon already used.
- **Vocab word compacted, difficulty inline (changed, Prompt #015 Task 3):** Word font size cut from `text-heading-1` (28px) to `1.25rem` (20px, ~30% smaller as specified); the difficulty badge moved into the same flex row as the word + 🔊 icon instead of sitting on its own line above.
- **Vocab pills recolored green (changed, Prompt #015 Task 4):** `bg-[#e8f5e9] text-[#2e7d32] border-[#81c784]`, hover `bg-[#c8e6c9]` — replaces Prompt #014's neutral border-only pill style.
- **LoadingSpinner preview words (added, Prompt #015 Task 5):** `LoadingSpinner.jsx` gained an optional `previewWords` prop rendering up to 12 real story words (filtered to length > 2) in small muted text below the existing spinner/message. `ListeningStoryView.jsx` wires this from `story?.vocabulary`, which `loadStory()`'s existing flow already populates via `setStory()` after the first (`/api/generate-story`) fetch resolves — well before `loading` itself flips false (that waits on the second `/api/story-questions` fetch too), so the preview genuinely appears mid-load without any new data-plumbing. This is a different feature from the SAC-021 word carousel removed in Prompt #012 — that one rotated a single word from a generic per-scenario pool; this one is a static list drawn from the actual generated story.
- **EXPLORATORY — micro-pauses after short words (Prompt #015 Task 6):** New `addMicroPauses(text, maxWordLength=2)` in `speechUtils.js` inserts a comma after any standalone word ≤2 letters without existing trailing punctuation, targeting reported short-word blurring (e.g. "y le dan la" reading as one slur). Given the real risk of destabilizing the onboundary/resume-from-boundary system (the subject of several dedicated bug-fix rounds in v1.0g/h), this was scoped carefully rather than applied as a blind text-preprocessing step: `speakSentenceAt` and `resumeSentenceFromBoundary` were both routed through one new `getSpokenText(idx)` helper so they always agree on the exact same padded text for a given sentence index — since onboundary charIndex offsets captured while speaking one get used to slice the other for mid-sentence resume, padding one but not the other would have shifted resume positions out of sync with what was actually spoken (verified this risk with a dedicated boundary-simulation test before and after: resume now slices a clean, correct suffix of the padded text). `playTranscriptSentence` (no resume tracking) also uses the padded text. Deliberately not touched: `ConversationView.jsx`/`VocabularyMatching.jsx` (single words/full free-form Claude responses, not the same sentence-playback engine or blur risk profile) — matches the prompt's own scoping to `ListeningStoryView.jsx`.

**Decisions made without an explicit spec, Prompt #015:** (1) Version numbering — treated "target v1.0n" from #013/#014 as a working label that was never actually shipped as its own release (the badge stayed v1.0m throughout), so bumped straight to v1.0o as this prompt explicitly instructed, rather than inserting a v1.0n release that never happened. (2) Task 1's fix required adding a `speakTokenRef` guard to two files (`ConversationView.jsx`, `VocabularyMatching.jsx`) that didn't have a token-guard pattern before — not explicitly asked for, but necessary so the new setTimeout-delayed `speak()` calls can't fire a stale/superseded utterance after a rapid second call (the same class of race Prompt #014 found and fixed three of, applied preemptively here rather than waiting to discover it via a repro). (3) Same fundamental limitation as #013/#014: speech clarity/quality still cannot be verified in this environment. This applies with extra force to Task 1 (does the delay actually stop the clipping) and Task 6 (does the micro-pause experiment sound better, worse, or just different) — both are explicitly framed in the prompt itself as needing a human ear, Task 6 with its own "revert if it sounds worse" escape hatch. Built both faithfully, verified everything programmatically checkable (real cached backend sentence containing "y le dan la" confirmed to render as "y, le, dan la," post-fix), and stopped at the commit/deploy gate exactly as instructed.

### Prompt #016 additions (v1.0o → v1.0p)

This is the first round in the Phase 2.7 arc built directly from a real local listen-test of the previous round, rather than shipping more unverified changes on faith.

- **Task 6's micro-pauses (Prompt #015) reverted entirely (Prompt #016 Task 1):** Confirmed by listening that "y, le, dan la," sounded unnatural. `addMicroPauses()` removed from `speechUtils.js`; `ListeningStoryView.jsx`'s `getSpokenText()` indirection removed along with it — `speakSentenceAt`, `resumeSentenceFromBoundary`, and `playTranscriptSentence` all read `sentences[idx].spanish` directly again, exactly as before Prompt #015 touched them.
- **Syllable-loss delay raised 75ms → 150ms (Prompt #016 Task 2):** Confirmed by listening that 75ms wasn't enough. Per the prompt's own ordering ("try Approach A first, then B — a silent warm-up utterance — only if needed"), only the delay increase was built this round; stacking an untested warm-up fix on top would make it impossible to tell which change (if either) actually worked on the next listen. Raised again to 175ms in Prompt #017 below — Approach B (warm-up utterance) is still documented and unbuilt if that turns out not to be enough either.
- **Vocab word auto-play moved from "after correct match" to "on word appearance" (fixed, Prompt #016 Task 3):** Prompt #015's design (play the word once the user gets it right) had the order backwards — a learner needs to *hear* the word before matching it, not as a reward after. New `useEffect` in `VocabularyMatching.jsx` keyed on `currentIdx` auto-plays via the existing `playWord()` ~200ms after a new word appears (including the very first one, and every subsequent one via "Next →"); wrong guesses don't retrigger it since they don't change `currentIdx`. The old `autoplayTimeoutRef` (needed for the post-match design) was removed rather than repurposed — `useEffect`'s own cleanup function already handles clearing the pending timer on rapid word changes.
- **Loading spinner: animated one-at-a-time words instead of a static list (rewritten, Prompt #016 Task 4):** `LoadingSpinner.jsx`'s `previewWords` prop (added Prompt #015) now drives a cycling single-word display instead of a static "12 words joined by a dot" line — new `.loading-word` / `@keyframes wordAppear` (fade + scale) in `index.css`, first word as soon as data's available, advancing every 700ms. **Real bug found and fixed via testing, not listening:** the first draft tried `startTimer.interval = interval` to stash the `setInterval` ID somewhere the cleanup function could reach it — this throws in a browser (`Cannot create property 'interval' on number '3'`), since browser `setTimeout`/`setInterval` return plain numeric IDs, not the object Node.js's timer functions return (which *can* carry attached properties — a Node/browser API difference easy to miss when the mental model comes from server-side JS). Fixed by holding the interval ID in an ordinary closure variable instead. Caught by the zero-console-errors check; confirmed the actual fix (not just the silenced error) by verifying the word genuinely advances after the timing fix.
- **Loading message updated to "30 to 40 seconds" (Prompt #016 Task 5):** `LoadingSpinner.jsx`'s default `estimateText` only — `ConversationView.jsx`'s two calls pass their own accurate override for that mode's lighter API calls and were untouched.

**Decisions made without an explicit spec, Prompt #016:** (1) Deliberately did not build Task 2's Approach B (warm-up utterance) preemptively alongside Approach A, even though it would have been easy to add — the prompt's own instructions frame this as a sequential, listen-gated decision ("try A, then B if needed"), and building both at once would make the next listen-test unable to isolate which fix (if either) actually worked. (2) Same fundamental audio-perception limitation as every prior round in this arc: verified the delay is genuinely 150ms now and the reverted sentence text is genuinely unpadded again (both via mocked-timing tests and a real-backend smoke test), but whether syllables are actually crisp is still Vinay's call alone.

### Prompt #017 additions (v1.0p → v1.0q)

- **Syllable-loss delay raised again, 150ms → 175ms (Prompt #017 Task 1):** 150ms (Prompt #016) confirmed still occasionally clipping on a real listen — this is a small, targeted constant change, same `SPEAK_START_DELAY_MS` in `speechUtils.js`. Approach B (silent warm-up utterance, documented since Prompt #016) still hasn't been needed/built — if 175ms isn't enough either, that's the next thing to try.
- **Loading animation: slower cadence, 5 rotating styles, loops indefinitely (Prompt #017 Task 2, full rewrite of `LoadingSpinner.jsx`'s cycling logic):**
  - 2A: word interval 700ms → 1000ms.
  - 2B: 5 new keyframes in `index.css` (`wordFadeScale`/`wordZoomBounce`/`wordSlideUpFade`/`wordSpinIn`/`wordPulseGlow`, each with its own `.loading-word-<style>` class), selected per word via `ANIMATION_STYLES[tick % 5]` so consecutive words visibly use different effects.
  - 2C: the state driving the cycle changed from a bounded `wordIdx` (stopped once every word had shown once, Prompt #016's design) to an unbounded `tick` counter, with the displayed word now `previewWords[tick % previewWords.length]` — both the word and the animation style wrap independently off the same counter, so the whole thing loops for as long as the component stays mounted rather than freezing on the last word once the list runs out. No explicit "stop" logic was needed for when the story finishes: `LoadingSpinner` simply stops being rendered at that point (its parent's `loading` branch changes), which lets the effect's existing cleanup function clear the interval automatically — same unmount-driven cleanup pattern already used everywhere else in this codebase.
  - Verified directly (not inferred from believing the code "should" work): fed a synthetic 3-word list through a real browser session and polled it every 1000ms for ~7s, confirming the word sequence actually wraps (`restaurante → amigo → camarero → restaurante → amigo → camarero`) with the animation style independently wrapping every 5th tick — both moduli behaving exactly as designed, on desktop and mobile alike.

**Decisions made without an explicit spec, Prompt #017:** Same fundamental audio-perception limitation as every round in this arc — confirmed programmatically that the delay is genuinely 175ms and that the animation timing/cycling/looping all work exactly as specified, but whether 175ms is *actually* enough to stop syllable clipping is still Vinay's call. Given this delay has now moved in 3 consecutive rounds (75→150→175ms) purely from listen-test feedback each time, if it needs raising a 4th time it's probably worth pointing out to Vinay that Approach B (the warm-up utterance) has been sitting ready and unbuilt since Prompt #016 — it may be a more structural fix than continuing to nudge the same constant.

---

## Spanish Learning Philosophy (Embedded in App Design)

Vinay's stated learning goals (prioritized):
1. **Understand native speakers** (comprehension)
2. **Be comprehensible to native speakers** (broken Spanish OK, as long as meaning is clear)
3. **Improve grammar & pronunciation** (after first two goals are solid)

**Design implication:** v1.0b is optimized for goals #1 and #2. Feedback is encouraging, not harsh. App accepts imperfect Spanish as long as it's understandable. Grammar corrections are suggestions, not roadblocks. This is intentional — perfect grammar before confident speaking would be backwards.

---

## Known Issues & Testing Needed

### Issue 1: History Dashboard "0 Total Sessions" reports — investigated 2026-08-25, likely not a bug
- Reported as: History shows "0 Total Sessions" on some devices even after completing sessions.
- **Root cause found:** re-tested against production using Playwright's WebKit engine (Safari's actual storage/rendering engine, iPhone 13 emulation) — a session saved and completed in one browser context correctly appeared in History in that same context; a **separate, fresh browser context with no prior session correctly showed 0**, which is expected. IndexedDB is per-origin *and* per-browser-profile: sessions completed on one device/browser/private-window never appear on another. Not reproduced as an actual bug where a device that *had* completed a session still showed 0.
- Test needed to actually confirm a bug (not yet done): same device, same regular (non-private) browser window, complete a session, immediately check History without switching context.
- Impact: Unconfirmed — likely a false alarm, not medium.
- Next step: only re-open as a real bug if reproduced with the specific repro steps above.

### Issue 2: Audio Playback Inconsistency (Mobile vs. Web) — resolved for Listening Mode (v1.0s, SAC-037); still open for Conversation Mode
- Web version auto-plays; mobile Safari may not.
- **Root cause (code-level, not device-confirmed):** `speechSynthesis.speak()` called from inside an async chain after a mount effect (`useEffect` → `await fetch(...)` → speak), never synchronously inside a click handler. Mobile Safari enforces a "user activation" window for speech synthesis that's very likely expired by the time these async calls fire — this would silently no-op with no visible error, matching the reported "web works, mobile doesn't" pattern exactly.
- **Listening Mode: structurally resolved, not just worked around (Prompt #020, SAC-037).** `ListeningStoryView.jsx` no longer auto-plays anything, ever — a mandatory "Tap to Begin" gate means the *first* `speak()` call is always a direct consequence of a real click, which eliminates the async-chain autoplay-policy problem this issue describes rather than just detecting and recovering from it (SAC-024's older "Tap to Play" fallback, v1.0l, is now fully removed as a result — nothing left for it to catch).
- **Conversation Mode: still has the original issue, untouched by this round.** `ConversationView.jsx`'s `startConversation()` still calls `playSpanishAudio()` from inside an async chain (`useEffect` → `await fetch('/api/initiate')` → speak) with no equivalent gate — Prompt #020's file scope was `ListeningStoryView.jsx`/`VocabularyMatching.jsx` only. Same root-cause theory as before, still not device-verified (Playwright's WebKit test build doesn't implement `speechSynthesis` at all).
- Impact: Low for Listening Mode (resolved). Medium for Conversation Mode (unchanged — UX friction, or a fully silent app on mobile if the theory is right).
- Next step: if this is worth fixing for Conversation Mode too, the same "require a tap before the first speak()" pattern from SAC-037 would resolve it the same way — not yet requested or built.

### Issue 3: Mobile UX Needs Polish
- Navigation buttons too small, layout cramped, excessive scrolling.
- Root cause: designed desktop-first; mobile-specific CSS incomplete.
- Test needed: real iPhone/Android — test all buttons, measure tap targets (should be 44px+; some already are per the v1.0i design-token pass, but not audited end-to-end for mobile viewports specifically).
- Impact: Low (works, but awkward).
- Next step: SAC-022 (mobile tap targets), SAC-023 (mobile header) — Phase 1.

### Issue 4: Favicon 404 on every page load — cosmetic, pre-existing, not yet fixed
- `index.html`'s `<link rel="icon" href="/vite.svg">` still points at Vite's scaffolding default — nothing in the repo ever replaced it with a real app icon, and no `public/vite.svg` file exists, so it 404s in the browser console on every load (confirmed live on production during the v1.0x SAC-069/070 deploy's post-deploy verification pass).
- Impact: Cosmetic only — doesn't affect any functionality, just an extra console line and a blank browser tab icon.
- Next step: not yet requested; would need a real favicon asset added to `public/` and the `<link>` tag updated to match.

---

## Data Integrity Rules

**Phase 1 (v1.0a-v1.0i):** Not applicable — app was stateless, no shared/derived records, no users, no database.

**v1.0j onward:** IndexedDB session history added (SAC-013). Checked `~/.claude/skills/PERSONAL_STYLE.md` (the file CLAUDE.md anticipated referencing here) — its persistence guidance ("clear-then-insert, identity matching, synthetic test data restrictions") is written for shared, multi-user databases. It doesn't apply here: this is local-only, per-browser IndexedDB with a single implicit "user" (whoever's using that browser), no shared/derived records, no synthetic-vs-real-data distinction to worry about. No special data-integrity process needed beyond the usual (don't silently swallow save failures — `db.js` callers log to console on error).

---

## Versioning Convention

- Format: `v[major].[minor][letter]`
- **Bump on every change**, even minor ones (v1.0b → v1.0c → v1.1a, etc.)
- Version must be **visibly displayed** in running app (header badge, top right)
- **Tell the user the new version number after every shipped change.**
- Current version: **v1.0x**

---

## Phased Roadmap

### Phase 1 — MVP (Current)
- [x] Audio conversation interface (Web Speech API)
- [x] Scenario selector (flexible, Claude-generated)
- [x] Claude initiates conversation
- [x] User speaks Spanish → Claude responds + feedback
- [x] Backend proxy (CORS fix)
- [x] Local testing complete
- [x] GitHub repo + deploy to Netlify + Railway — shipped v1.0k (CLI-deployed, not GitHub-auto-deploy yet — see SAC-017)
- [x] Final documentation (README, CLAUDE.md, PENDING.md) — updated for v1.0k

### Phase 2 — Enhanced (Do Not Build Yet)
- [x] Session persistence (IndexedDB) — shipped v1.0j
- [x] Basic analytics logging — shipped v1.0k (console-only, no provider yet)
- [x] Mobile responsiveness polish (Phase 1 pass) — shipped v1.0l (SAC-022/023)
- [x] Audio autoplay fallback — shipped v1.0l (SAC-024; underlying browser-policy root cause still unverified against a real device)
- [ ] Email capture live (code shipped v1.0k, inactive — needs a real `VITE_FORMSPREE_URL`)
- [ ] Auto-deploy on push (Netlify + Railway connected to GitHub) — SAC-017
- [x] Reorder main screen, Listening first — shipped v1.0m (SAC-025)
- [x] Progress bar jump-to-sentence markers — shipped v1.0m (SAC-026)
- [x] Vocabulary matching redesign (one-at-a-time) — shipped v1.0m (SAC-027)
- [x] Vocabulary context (example phrases) — shipped v1.0m (SAC-028)
- [x] Story caching + regenerate button — shipped v1.0m (SAC-029, cache is ephemeral on Railway — see Tech Stack)
- [ ] Difficulty selector — SAC-030
- [ ] Scoring system (accuracy + fluency)
- [ ] User progress dashboard
- [ ] Export transcript after session

### Phase 3 — Platform / Commercial (Do Not Build Yet)
- [ ] Multi-user with leaderboards
- [ ] Streak system + gamification
- [ ] Integration with Duolingo-like XP system
- [ ] Voice accent analysis
- [ ] Conversation history across sessions (Postgres)
- [ ] Advanced AI features (error pattern tracking, personalized coaching)

---

## Coding Conventions

**React / JavaScript:**
- Functional components with hooks (useState, useRef, useEffect)
- No TypeScript in Phase 1 (plain JS for speed)
- API keys never in frontend code — always `.env` on backend
- Error handling: catch + surface errors to UI (not silent failures)
- Component naming: PascalCase (ConversationView.jsx)
- File names: match component names (App.jsx, ScenarioSelector.jsx)

**UI / Styling:**
- Tailwind CSS, light backgrounds (clean + white, per Vinay's preference)
- Safe-first UX: lead with what user CAN do, not restrictions
- Plain-language copy (no jargon: "Start" not "Initialize")
- Friendly, conversational tone in UI text
- **Design tokens (added v1.0i):** colors, type scale, and radii are defined once as CSS variables in `src/index.css` `:root` and mapped into `tailwind.config.js`'s `theme.extend` — use the resulting utility classes (`bg-primary`, `text-ink-muted`, `bg-surface`, `rounded-card`, `text-heading-2`, etc.), never a raw Tailwind palette color (`bg-blue-600`, `text-gray-500`) or inline hex, so the whole app stays on one palette. Primary = teal, secondary = coral; success/danger/warn are semantic, not tied to a specific hue name.

**Backend (Node.js):**
- Express routes organized by endpoint (/api/initiate, /api/respond)
- CORS middleware allows localhost:5173 (dev), production URL (prod)
- Explicit error responses (not silent failures)
- Logging to console (console.error, console.log)

---

## Known Issues & Lessons Learned

- **CORS block from local file (FIXED in v1.0b):** Initial HTML artifact hit CORS error. Root cause: browsers block direct API calls from `file://` URLs. Solution: backend proxy pattern. This is production-grade and required for any deployment.

- **Web Speech API browser inconsistency:** Chrome/Edge are most reliable. Firefox has spotty support. Safari (iOS) requires HTTPS (won't work on localhost, needs production URL). Test on target browsers before declaring a fix.

- **TTS speech rate (0.8x) varies by device:** Slower rate helps beginners but may sound unnatural on some systems. Parameter is in ConversationView.jsx line ~98 (`utterance.rate = 0.8`). May need tweaking per user feedback.

- **Model string stale risk:** claude-opus-4-8 is current as of Jan 2025. If API calls start failing silently (no error, but no response either), **check the model string first** before assuming the bug is elsewhere. This has been a recurring issue across projects.

- **Speech recognition empty transcript:** Web Speech API fires `onend` even if user says nothing or audio is noise. Handle gracefully by checking `transcript.trim()` before calling handleUserResponse.

- **API key not loading from .env (FIXED in v1.0b):** Initial setup had .env in wrong folder (parent instead of spanish-audio-chat subfolder). Backend needs .env at project root. If backend shows "⚠️ ANTHROPIC_API_KEY not set in .env", first check: is .env in the right folder?

- **Intermittent UTF-8 replacement character in accented Spanish output (observed v1.0c, unresolved):** Occasionally (roughly 1 in 3-5 calls in testing) `/api/respond`'s JSON output contains a single U+FFFD replacement character in place of an accented letter (e.g., "café" → "caf�"), most often inside the `errors[].userSaid` field. Confirmed via direct Anthropic SDK calls (bypassing Express entirely) that this originates from the model's own generation, not from `server.js`'s regex/JSON-parsing or Express's response serialization — isolated repro scripts calling the SDK directly with the identical prompt reproduced it at a similar rate. `JSON.parse` still succeeds (U+FFFD is a valid JSON string character), so it doesn't break the app — it's a rare cosmetic glitch, not a functional bug. Not worth chasing further unless it starts appearing more visibly or the user requests a workaround (e.g., asking the model to `\u`-escape non-ASCII output).

- **JSON-generating endpoints need a generous `max_tokens` (learned v1.0d):** `/api/generate-story` truncated mid-JSON at `max_tokens: 1500` (`stop_reason: 'max_tokens'`) because the full-word `vocabulary` array for a 150-200 word story is itself 50-90 entries. Bumped to 4000 and added a `console.warn` when `stop_reason === 'max_tokens'` so future truncation shows up in backend logs instead of surfacing only as a confusing `JSON.parse` error. Worth checking `max_tokens` headroom on any future endpoint that asks the model to enumerate something proportional to input length (e.g. per-word, per-sentence).

---

## Deployment

**Local Dev:**
```bash
# Terminal 1 — Frontend
cd spanish-audio-chat
npm install  (first time only)
npm run dev
# Runs on http://localhost:5173

# Terminal 2 — Backend
cd spanish-audio-chat
npm run backend
# Runs on http://localhost:3000
```

**Production (live as of v1.0k — CLI-deployed, not yet GitHub-auto-deploy, see SAC-017):**
- Frontend: https://spanish-audio-chat.netlify.app
- Backend: https://spanish-audio-chat-production.up.railway.app
- GitHub: https://github.com/vvaidya98/spanish-audio-chat (public)

To ship a change to production, `git push` is not sufficient by itself — also run:
```bash
# Backend (from spanish-audio-chat/)
railway up --detach --service spanish-audio-chat
# after any env var change: railway redeploy --service spanish-audio-chat --yes (or another `railway up`)

# Frontend
VITE_API_URL="https://spanish-audio-chat-production.up.railway.app" npm run build
netlify deploy --prod --dir=dist
```
Both CLIs are pre-authenticated in this environment (`railway whoami` / `netlify status`) as of v1.0k — don't re-run the device-code login flow unless a CLI reports unauthenticated.

**CORS for Production:**
- Backend's `FRONTEND_URL` env var (Railway) is set to the Netlify URL above; `server.js`'s CORS `origin` list is `[localhost:5173, FRONTEND_URL]`
- Frontend's `VITE_API_URL` (Netlify build env var) points at the Railway URL — resolved via `src/api.js`'s `apiFetch()`, not hardcoded per-call

---

## Decisions Log

- **2026-08-24 (Claude.ai scoping):** Chose Web Speech API (native, no external libs) over external speech library. Chose backend proxy over direct frontend-to-API calls (CORS fix). Chose Flask/FastAPI + Railway as backup if Node.js has friction, but Node.js is default.
- **2026-08-24 (during local testing):** Moved index.html to root (not in public/) to match Vite's default config. Placed .env in spanish-audio-chat subfolder (not parent).
- **2026-08-24 (design decision):** Feedback timing is after each turn (not mid-conversation). Claude provides Spanish response, then English feedback. This pacing respects the learning flow.
- **2026-08-24 (Prompt #001 — SAC-010 scoping):** SAC-010 was originally reserved in PENDING.md for "session persistence" (Phase 2, planned to start after v1.0c's deploy milestone shipped). Prompt #001 redefined SAC-010 as the listening-first, multi-turn conversation-flow overhaul instead, and shipped it as v1.0c directly — ahead of the GitHub/Netlify/Railway deploy milestone (SAC-003–008), which is deferred, not dropped. Session persistence was renumbered to SAC-011. Confirmed with Vinay before building (per PENDING.md's standing instruction to check in on new/renumbered work before starting it).
- **2026-08-24 (SAC-010 build):** "End Conversation" button coexists with "Tap to Speak" once 5 exchanges are complete (both shown, user chooses), rather than replacing it outright — preserves the intended 5-8 exchange flexibility. "Tap to Speak" is only removed once the hard cap of 8 exchanges is reached, at which point "End Conversation" is the only option.
- **2026-08-24 (Prompt #002 — SAC-014 numbering):** PENDING.md's highest ID was SAC-011 (session persistence) when Prompt #002 arrived using SAC-014 for Listening Story Mode and SAC-013 for session persistence, skipping SAC-012. Confirmed with Vinay: followed the prompt literally — session persistence renumbered SAC-011 → SAC-013, SAC-012 left as an intentional gap, SAC-014 assigned to Listening Story Mode. Note: PENDING.md was also independently edited by Vinay's other (middle-panel) Claude Code session between prompts in this project — same file, two active sessions per his split-panel workflow — which is worth checking for on every read, not just assuming this session's last write is still current.
- **2026-08-24 (Prompt #002-A — incomplete, build paused):** Prompt #002-A (SAC-014-A/B/C: transcript sentence-tooltips, audio pause/resume/restart controls, MCQ tooltips) arrived truncated mid-example in part A, with no detailed requirements for B/C and no testing/reporting/acceptance-criteria sections (every other prompt in this project has included these). Held off building against a guessed spec — asked Vinay to resend the complete prompt. Recorded the partial spec in PENDING.md so it isn't lost.
- **2026-08-24 (Prompt #002-A completed, shipped v1.0e):** Vinay resent the rest of the prompt (it picked up mid-sentence from where the original cut off). Confirmed target version was v1.0e (stated in both the REPORTING and ACCEPTANCE CRITERIA sections of the completed prompt, overriding the original truncated header's "still v1.0d"). Built SAC-014-A/B/C in full — see Key Architecture Decisions above for the `HoverableText.jsx` and sentence-driven playback engine design. Testing caught one real bug (React DOM-nesting warning from a `<div>`-rendering `HoverableText` inside a `<p>`), fixed before shipping.
- **2026-08-24 (Prompt for SAC-014-D — arrived missing its opening, built anyway):** Unlike Prompt #002-A's truncation, this one was missing its start (no header/context, no implementation detail for the two reported bugs) but had complete, actionable detail for everything else (exact target ranges for the pause-duration fix, full specs for the hover-badge and story-length changes, full testing/reporting/acceptance-criteria sections). Judgment call: proceeded without asking, since the gap was fillable — Bug #2's fix was fully specified via its testing step's target range, and Bug #1 was diagnosable directly from the existing code (see Key Architecture Decisions above: StrictMode double-invoke race). This differs from #002-A, where the missing middle contained substantial new-feature spec (audio pause/resume mechanics, exact button behavior) that genuinely couldn't be inferred. Reconstructed the header context (SAC-014-D, v1.0f) from internal references ("v1.0f" in Reporting/Acceptance, "SAC-014-D" in Reporting, "SAC-014-E" in the Phase-2-parking section).
- **2026-08-24 (Prompt for SAC-014-E — arrived missing its opening again, plus an ID collision):** Same pattern a third time: no header, cut off mid-way through an icon-styling subsection, but everything needed to build was either fully specified (shorter-sentence target range, icon glyphs literally given in testing steps, progress-bar label format) or a defensible engineering judgment call (the `onboundary`-based smooth-speed-change mechanism — no implementation guidance given, root Web Speech API limitation understood from the v1.0e/f work). Separately: PENDING.md already had an unbuilt, parked "SAC-014-E" (story caching) from an earlier round, but this prompt's own Reporting section called the *current* work "SAC-014-E" too. Renumbered the parked caching idea to SAC-014-F to resolve the collision — a low-stakes call since that idea was never built, so didn't stop to ask (unlike the SAC-010/SAC-013 renames, which involved IDs already referenced elsewhere as actively-tracked work).
- **2026-08-24 (Prompt for SAC-014-F — missing opening a fourth time, ID collision a second time):** Same pattern again: no header, cut off mid-way through an icon-sizing subsection. Both "Fix #1" (stop button) and "Fix #2" (speed-change skip) testing steps described symptoms with zero implementation guidance — both root causes were diagnosed directly from the v1.0g code (see Key Architecture Decisions: token-guarded speaking engine). And again, this round's own Reporting section called itself "SAC-014-F" — colliding with the *previous* round's rename of the parked story-caching idea to that same ID. Renumbered the caching idea again, to SAC-014-G, same low-stakes reasoning as before. Pattern worth naming explicitly at this point: every SAC-014 sub-round so far has arrived missing its opening section, and the parked "next available ID" keeps getting claimed by the following round before it's ever built — if this keeps happening, consider just leaving the caching idea unnumbered in the parking area until it's actually scheduled, rather than pre-assigning an ID it won't keep.
- **2026-08-24 (Prompt for SAC-014-H — most compressed yet, no ID this time):** This prompt was *only* its Reporting and Acceptance Criteria sections — no header, no Objective, no Implementation section at all, the most compressed of any round so far. Unlike prior rounds, it never referenced its own SAC ID anywhere, so there was no collision to resolve this time — assigned SAC-014-H as the next available ID (following the already-claimed SAC-014-G). What made building without asking still defensible: the Acceptance Criteria bullets were unusually concrete on their own — an exact 28/18/16/14px type scale, exact 1.5rem spacing, exact 44px tap targets, exact 0.2s transition timing, exact interaction semantics for vocabulary matching (click-to-pair, checkmarks, completion message) — closer to a real spec than most rounds' actual Implementation sections have been. Testing caught a self-inflicted test bug worth remembering for future rounds: an unscoped Playwright locator (`text=/Correct!|Try again/`) picked up the *first* matching element in DOM order, which was the MCQ section's own persistent "Correct! ✓" answer-feedback text (appearing earlier in the page) rather than the intended Vocabulary Matching card's transient banner — the log claimed a successful match that a screenshot then contradicted. Fixed by scoping the locator to the vocabulary card specifically before trusting a "test passed" result again.
- **2026-08-24 (Prompt for SAC-013 + SAC-015 — largest round yet, real persistence layer):** This prompt introduced actual client-side data storage (IndexedDB) for the first time in the project — the largest scope of any round so far (new storage module, two new full-page components, scenario list expansion, a dead-button fix). Given CLAUDE.md's own Data Integrity Rules section explicitly flagged "return to PERSONAL_STYLE.md rules" for this moment, checked that file before designing anything — see Data Integrity Rules above for what was found (its rules are for shared multi-user databases, don't apply to local-only per-browser storage). Also folded in SAC-013 itself, which had sat undefined in PENDING.md since v1.0c (originally just a one-line placeholder: "Objective: Store all sessions to IndexedDB. User can view history" — no schema, no UI spec) — this prompt's Testing/Acceptance sections were the first time SAC-013 actually got fleshed out, alongside the new SAC-015 (scenario/button/history-dashboard work). Design calls made without an explicit spec to follow: single `sessions` object store (not separate stores per mode) so the unified History Dashboard doesn't need to merge two queries; sessions save on explicit "leave" actions (End Conversation / header Back) rather than continuously, so a closed tab mid-session loses that session — a disclosed gap, not an oversight; vocabulary-match results stored as a count + word list rather than per-word matched/unmatched detail, trading review granularity for a simpler save payload.
- **2026-08-25 (production deployment round, v1.0k — first round needing external accounts/credentials):** This prompt (GitHub + Netlify + Railway deploy, email capture, analytics, launch checklist) arrived via a `/compact` message with only its back half attached (Email Capture through Post-Launch Tasks — no header, no Objective, no earlier numbered sections), consistent with the pattern of prior rounds. Unlike prior rounds, this one couldn't just be built from the codebase — it required real external accounts. Checked tooling first (`git status`, `gh`, `netlify --version`, `railway --version`): no git repo yet, no `gh` CLI, `netlify` CLI present and already authenticated as Vinay (linked to an unrelated site, `tripbrazil`), no `railway` CLI. Did everything possible locally without asking first (git init, MIT LICENSE, `src/analytics.js`, `EmailCapture.jsx`, version bump, local Playwright test), then used `AskUserQuestion` for the three genuinely external decisions: GitHub repo creation (Vinay created it manually and sent the URL — faster than installing/authenticating `gh`), Railway (installed the CLI, Vinay completed the browser device-code authorization — first attempt's code expired before use since the login command was accidentally wrapped in a sub-shell `&` that got killed when its parent script exited; second attempt with a bare `run_in_background: true` call stayed alive correctly), Formspree (skipped — Vinay chose not to create a form this round, so `EmailCapture.jsx` ships wired but inactive). Two real bugs hit and fixed during deployment (not app bugs, deploy-config bugs): Railway's Nixpacks auto-ran the frontend's `npm run build` and failed on a missing `terser` dependency (fixed with `railway.json` overriding build/start commands, and `terser` added as a devDependency since it's also needed for Netlify's own frontend build) — and a `.env` extraction script (`grep '^ANTHROPIC_API_KEY='`) silently produced an empty value because the file has leading whitespace before that key, which shipped a broken backend (auth error on every Claude API call) until caught by actually calling `/api/initiate` against the live URL rather than just checking `/health`. Lesson generalized into the Environment Variables section above. GitHub's repo-creation defaults added placeholder README/LICENSE/gitignore content despite requesting an empty repo — confirmed it was trivial single-line/template content (not real work) before force-pushing local history over it.
- **2026-08-25 (handoff/roadmap round, unnumbered):** Received a 13-item Phase 1-2 enhancement roadmap doc (not a numbered Prompt — one-line item descriptions, no Testing/Acceptance sections) meant to prime a new conversation thread. Its self-assigned IDs (SAC-016 through SAC-027, plus SAC-011-A) collided with already-shipped/active IDs (SAC-016 = analytics/email capture from the v1.0k round above, SAC-017 = GitHub auto-deploy, still open) — renumbered the whole batch sequentially to SAC-018–030 in PENDING.md rather than a confusing partial fix, same resolution pattern as the earlier SAC-014-E/F/G collisions. Investigated the doc's two reported bugs against production using Playwright's WebKit engine (Safari's real engine — Chromium alone can't speak to Safari-specific behavior): the "History showing 0 sessions" report turned out to most likely be an expectation mismatch, not a bug (IndexedDB is genuinely per-browser-profile — a fresh context correctly shows 0, which is what "0 sessions on this device" always looks like whether or not it's actually broken); the audio-autoplay report has a plausible code-level cause (async `speak()` calls outside any click handler, breaking mobile Safari's user-activation requirement) but could not be device-verified since Playwright's WebKit test build doesn't implement the Web Speech API at all. Given the roadmap's 7 Phase-1 items were only one-line descriptions (thinner than every prior round's Testing Requirements/Acceptance Criteria), asked Vinay how to proceed rather than guessing at 7 UI features' worth of undefined detail — he asked for full specs before building, so no Phase 1 code was written this round.
- **2026-08-25 (Prompt #009 — CLAUDE.md production-status update):** Five requested edits to CLAUDE.md: version line and live URLs were already correct from the v1.0k round above (updated the version line to the prompt's exact requested wording, "v1.0k (Live in Production)"); added the new Known Issues & Testing Needed section, but with the actual verified findings from the round above instead of the prompt's draft guesses (which predated that investigation) — corrected "IndexedDB may not work on mobile Safari" to reflect that it does, per-context isolation was the real explanation, and corrected the SAC references in that section to match PENDING.md's actual renumbered IDs (SAC-022/023/024) rather than the pre-collision numbers the prompt's draft still used; the requested v1.0j/v1.0k Decisions Log entries were already covered in much more detail by the two entries above, so didn't duplicate shorter versions of the same facts.
- **2026-08-25 (Prompt #010 — Phase 1 full build, v1.0l, largest single-round feature count yet):** Prompt #010 gave full Testing Requirements/Acceptance Criteria for 7 SACs (its own numbering: SAC-016–022), a level of detail every earlier round's Phase-1 handoff/roadmap lacked. First move was re-flagging the same ID collision already resolved in the prior diagnostic round: SAC-016/017 are taken (v1.0k analytics + GitHub auto-deploy), so built against the already-established SAC-018–024 renumbering instead of re-litigating it. Built all 7 in the doc's suggested batches (loading spinner → mobile polish → nav/story-flow/vocab → audio investigation), then a full test pass across three engines: desktop Chromium, Chromium mobile-viewport emulation (Android Chrome proxy), and WebKit with iPhone device emulation (Safari proxy, real WebKit rendering/storage engine — not just a resized window). Testing surfaced four real bugs before shipping, all fixed same-round: (1) the loading-spinner condition for `ConversationView.jsx` never actually rendered, since `currentState` flips `'starting'` → `'processing'` synchronously before first paint — screenshot evidence caught this, not just a code read; (2) the SAC-024 autoplay-failure detector false-flagged the legitimate inter-sentence gap as a stall, fixed by also checking `gapTimeoutRef`; (3) `VocabularyMatching.jsx`'s pre-existing (not new-this-round) pattern of calling the parent's `onProgressChange` from inside a `setMatched` updater triggered a real React "setState during render" warning, moved to a `useEffect`; (4) a genuine mobile-only layout bug — word+badge overflow/clipping in `VocabularyMatching.jsx` at 390px width — only visible in an actual WebKit screenshot, not from reading the Tailwind classes. Explicitly disclosed rather than implied: no physical iPhone/Android device was available, so every mobile claim in the report is backed by emulated-but-real engines (WebKit, Chromium mobile viewport), not actual hardware.
- **2026-08-25 (Prompt #011 — Phase 2 batch 1-2, v1.0m, first round with zero ID collisions):** Prompt #011's SAC-025–029 numbering matched PENDING.md's own renumbered IDs exactly — the first round since the handoff-doc collision where no re-flagging or renumbering was needed. Built in the prompt's stated order (025→026→027→028→029, with 027/028 combined since both touch the same `VocabularyMatching.jsx` "after correct match" moment). Testing needed unusually generous timeouts this round: uncached story generation plus the now-heavier post-SAC-028 `/api/story-questions` call (more fields requested, `max_tokens` 3000→4500) can take 40-60s combined in dev, further inflated by React StrictMode's double-invoke of the mount effect (fires two real Claude API calls per load in dev only — confirmed via backend logs showing "No cache hit" printed twice for a single first load). An initial 60s test timeout looked like a stall/failure; a longer wait (backed by isolating the cached endpoint's timing directly via `curl`, confirming 157ms vs. ~16s+ uncached) confirmed everything was actually working correctly — the same "test timeout too tight, not a real bug" pattern as the v1.0k and v1.0l rounds, worth continuing to watch for rather than mistaking real API latency for a broken feature.
- **2026-08-25 (Prompt #012 — Phase 2.5 polish, v1.0m final, arrived truncated then completed mid-turn):** Prompt #012 cut off mid-sentence in Task 3 ("File: index.html — Update `") with no content after, and a "Loading" task named in its own Build Order/Decisions header ("Option A — Honest Spinner") never got a Task section at all — the clearest case yet of a genuinely missing-content gap (not just a missing header, which earlier rounds routinely built through). Built the two fully-specified tasks (speed relabeling, "Back to Stories" navigation) immediately, then used `AskUserQuestion` rather than guessing at either an app-wide rename's file scope or an unspecified spinner redesign — Vinay chose to send the rest, which arrived as a follow-up message mid-turn (surfaced via a system-reminder wrapper, not a new conversation turn) with full detail for both remaining tasks. Notable judgment call in Task 4: the "Keep" list and target pseudo-code only specified a spinner + label + message, no word carousel — removed the v1.0l carousel entirely (and deleted the now-dead `scenarioVocab.js`) rather than assuming it should survive alongside the new minimal design, since nothing in the resent spec asked to keep it. Also made the new `estimateText` prop conditional per call site rather than hardcoding "10-15 seconds" everywhere as literally shown in the pseudo-code — `ConversationView.jsx`'s ~2-5s initial load gets "a few seconds" instead, since displaying "10-15 seconds" there would have reintroduced exactly the kind of dishonesty this task set out to remove. No version bump this round — the prompt's own header explicitly stated "Target Version: v1.0m (final polish before lock)," overriding the project's usual bump-on-every-change convention with a direct, explicit instruction.
- **2026-08-25/26 (Prompts #013 + #014 — Phase 2.6, critical speech fix + polish, target v1.0n):** Prompt #013 was explicit and unusual: "CRITICAL... this is your non-negotiable foundation" framing around speech articulation, plus a hard instruction not to deploy until Vinay personally confirmed clarity by listening — the first time in this project a round couldn't be self-certified before shipping, since audio perception is a genuine capability boundary here, not a degree-of-realism gap like every other disclosed testing limitation. Built the rate/voice-selection fixes, verified everything checkable in code, and stopped at the commit gate as instructed. Prompt #014 arrived next and its own content confirmed the loop closed correctly without Vinay ever having to say "yes it worked" explicitly — its Task 1 ("Issue: Speeds too slow") is itself the proof that #013's clarity fix succeeded, since the complaint changed category entirely (slurred → too slow) rather than persisting. Testing #014's 6 tasks surfaced three genuine, non-obvious timer/race-condition bugs (documented in detail above) — notably the third one (a stale wrong-guess timer wiping a subsequent correct match's UI) took several rounds of isolated-repro debugging to pin down, since the visible symptom was layers removed from the actual cause.
- **2026-08-26/27 (Prompts #015-#017 — Phase 2.7 continued, v1.0o/p/q, then shipped):** Three more rounds of the same listen-test-gated loop, each moving the syllable-loss delay up (75→150→175ms) purely from Vinay's own ears — #015 also added and then #016 fully reverted a micro-pause experiment after a real listen confirmed it sounded worse, exactly the outcome its own "revert if needed" escape hatch anticipated. #016 caught a genuine Node-vs-browser API bug (a `setTimeout` return value can't carry an attached property in a browser, only in Node) via the zero-console-errors check, not via listening. #017's loading-animation rewrite was verified directly against a real synthetic word list in a live browser session rather than just trusting the modulo math looked right. All five rounds (#013-#017) had sat at the commit/deploy gate together since none had shipped individually; Vinay then sent explicit literal deploy commands (not a numbered prompt) and they shipped as one commit + one Railway/Netlify push, verified live via Playwright against the production URLs. A near-identical deploy message arrived again shortly after — checked `git status` first rather than assuming it was a duplicate no-op, found the app code already live but a docs-only follow-up (updating CLAUDE.md/PENDING.md/README.md from "uncommitted" to "shipped" language) still pending; ran the same sequence again, which correctly picked up just the doc changes and redeployed identical app code (Netlify's own diff confirmed "0 files" changed).
- **2026-08-27 (Prompt #018 — In-Memory Story Caching, v1.0r):** Replaced the v1.0m filesystem cache with an in-memory `Map`, exactly as specified. Verified the fix at the API level directly (`curl` timing: 0.11-0.12s cache hit vs. ~18-21s miss, byte-identical cached response, independent cache entries per scenario string, `regenerate` correctly bypassing and overwriting the cache) — not just "should work from reading the code." Full end-to-end UI testing then surfaced a real gap worth flagging rather than quietly ignoring: the prompt's own testing checklist expects a "<1s instant" second load, but `/api/story-questions` (MCQ + vocabulary data) has never been cached at all — a pre-existing, deliberate scope decision from the original v1.0m caching round, not something #018 touched. Since `loadStory()` waits on both endpoints before clearing `loading`, the un-cached ~12s `story-questions` call is now the dominant cost on every repeat load regardless of this fix. Logged as SAC-033 rather than silently expanding this round's scope to also cache it — the prompt's file/endpoint scope named only `/api/generate-story`, and the established pattern in this project is to flag scope gaps for Vinay to decide on rather than assume he wants them folded in.
- **2026-08-27 (Prompt #019 — SAC-033, cache `/api/story-questions` too, still v1.0r):** Vinay's next prompt confirmed the SAC-033 gap flagged in #018 and asked to close it, using the identical `Map` pattern — no version bump this round, added to the same v1.0r build rather than becoming v1.0s (the prompt's own header was explicit: "add to existing in-memory cache from #018"). Verified end-to-end via a real browser session, not just isolated API timing: a "go back → reload same scenario" click-through dropped from #018's measured ~25s down to **91ms total**. The prompt gave an explicit tradeoff decision to follow rather than solve: on Regenerate, leave the cached questions/vocab alone rather than invalidating them ("Option B for now... document as future improvement") — confirmed this is exactly what happens via the backend's own log sequence (`Regenerating story` → new story cached → `Using cached questions` for the *old*, untouched entry), and that the mismatched-but-valid old data doesn't crash anything (MCQ/vocab both still render). Logged the real fix (invalidate questions cache on regenerate) as SAC-034 rather than building it unasked, since the prompt was explicit that Option B was the intended scope for this round.
- **2026-08-27 (Prompt #020 — SAC-034/035/036/037, v1.0s, largest single-round scope since v1.0l):** Four SACs in one round: closed the SAC-034 gap flagged in #019 (questions cache now cleared on regenerate — verified via real timing, a post-regenerate `/api/story-questions` call took ~26.6s, a genuine fresh generation, not a cache hit); added a 5-element story-diversity instruction block to the generation prompt (SAC-035, verified by diffing a real regenerated story against the original — genuinely different, not just "should be" per the prompt engineering); swapped Vocabulary Matching to render before the MCQ Comprehension Check (SAC-036A) and suppressed the first vocab word's auto-play while keeping its 🔊 icon and every subsequent word's auto-play working (SAC-036B); and replaced all autoplay in Listening Mode with a mandatory "Tap to Begin" gate (SAC-037), which turned out to make an entire existing mechanism (SAC-024's "Tap to Play" autoplay-failure detector, v1.0l) dead code — removed it rather than leaving it disabled alongside the new gate, since a click-gated `speak()` call structurally can't hit the silent-autoplay-failure case that detector existed to catch. One test-script timing bug caught and correctly walked back before being reported as real: an early pass showed the first word's 🔊 icon appearing not to work, but a dedicated poll-every-50ms repro showed the actual `speak()` call landing ~221ms after the click (the existing 175ms delay plus overhead) — the original test only waited 150ms, so it was checking before the (successful) call had actually landed. Updated the Known Issues section to reflect that Issue 2 (audio autoplay inconsistency) is now structurally resolved for Listening Mode but explicitly still open for `ConversationView.jsx`, which this round's file scope never touched — worth remembering that "fixed audio autoplay" as a blanket claim would have been wrong; it's fixed in one of the app's two modes.
- **2026-08-27 (Prompt #021 — SAC-038 through SAC-045, v1.0t, 8 SACs, largest single-round scope in this project):** A UI/UX polish bundle spanning `ModeSelector.jsx`, `App.jsx`, `FooterNav.jsx`, `VocabularyMatching.jsx`, and a full layout rewrite of `ListeningStoryView.jsx`. Two notable judgment calls, both disclosed rather than silently decided: (1) SAC-041 and SAC-042 each gave a mockup showing exactly 4 player-control buttons (Prev/Play-Pause/Next/Last) — one fewer than the app had, since the 🔄 Replay button (added Prompt #013) wasn't in either mockup. Read the consistency across two independent sections of the same prompt as intentional, not an oversight, and removed Replay along with its handler; separately chose to keep the app's existing, already-differentiated icons (⏮/⏭/⏩) for the 4 remaining buttons rather than the prompt's own "◀/▶/▶/⏭" notation, since reusing "▶" for both Play and Next would have made two adjacent buttons look identical — a real usability regression a literal reading would have introduced, not something the prompt was likely asking for given it was typed as informal ASCII art rather than exact Unicode. (2) SAC-045's "checkbox above progress bar" instruction initially looked to conflict with SAC-041 moving the progress bar to the bottom — resolved by reading SAC-041's own layout diagram carefully, which actually keeps `[Progress bar]` in its own line, separate and higher up, with only the interactive buttons (not the passive progress/status display) relocating to the fixed bottom bar. Once read correctly, SAC-045's instruction applies exactly as written with no conflict. SAC-044's Home/Topics functional overlap (both now go to the scenario picker) was implemented literally as the prompt's own testing checklist describes rather than second-guessed, with the apparent redundancy flagged in PENDING.md for Vinay to weigh in on. One test-script false negative caught before being reported: a CSS-position check walked too few DOM ancestors and landed on a `static` wrapper instead of the actual `fixed` bar — verified directly via a full ancestor walk plus an on-screen scroll test (button position unchanged, 717.5px before and after scrolling 300px) before concluding the bar genuinely is fixed and the test itself just needed a longer selector chain.
- **2026-08-27 (Prompt #021 follow-up — 3 corrections + a real bug from Vinay's own testing, still v1.0t):** Three quick corrections (leftmost button → "First sentence" with jump-to-start behavior mirroring "Last sentence" rather than stepping back one; dynamic Play/Pause label; Home reverted from the scenario picker back to the Mode Selector, undoing SAC-044's Home/Topics redundancy). The Home fix itself introduced a real bug, caught by Vinay actually using the app rather than by any automated test: `handleFooterHome` had `if (activeViewRef.current?.back) { activeViewRef.current.back(); return }` — an early `return` copied from the one-step-back `handleFooterBack`/`handleFooterTopics` pattern, which meant Home only reached the Mode Selector from the scenario picker (where `activeViewRef.current` is null, so the early return never triggers) but not from inside an active story (where it does trigger, short-circuiting before `handleBackToModes()`). Exactly matches what Vinay reported: "if I'm on a specific story, Home doesn't go all the way back; if I'm already on the list of stories, it does." Fixed by removing the early return, restoring the original pre-SAC-044 `handleFooterMode` pattern where the session-save call never short-circuits reaching the Mode Selector. Verified via a real click-through from inside an active story (now reaches Mode Selector) and confirmed the session still saved correctly on the way out (History showed the completed session). Worth remembering: this bug passed the prior round's own automated Playwright tests because those tests happened to exercise Home from the scenario picker, not from inside an active story — a reminder that testing the same button from every distinct app state it can be reached from matters, not just confirming it works from one state.
- **2026-08-27 (Prompt #022 — SAC-046/047/048, v1.0u, Phase 2.9 UX refinements):** Regenerate Story relocated to a small button near the bottom, and `ListeningHeader.jsx` deleted outright rather than left as an empty shell — it only ever rendered that one button, so once nothing called it with a truthy `onRegenerate` it had no reason to exist (same "remove dead code, don't leave it disabled" pattern as SAC-024's removal in Prompt #020). The single "Display transcription" checkbox + 🌐 reveal-toggle became two independent "Display Spanish"/"Display English" checkboxes — a genuine simplification, not just a relabel: the old design's reset-on-sentence-change effect for the English reveal is gone entirely, since the new checkboxes are persistent preferences rather than per-sentence state. Play button gained an infinite pulse animation (`play-button-pulse`, CSS `scale()` keyframes) until the user's first real click or a regenerate, to compensate for Prompt #020 having removed the old "Tap to Begin" screen that used to make the entry point obvious. One test-tooling wrinkle worth remembering for future animated elements: Playwright's default `.click()` refuses to click continuously-animating targets at all (`element is not stable`, since a `transform: scale()` loop never stops moving long enough to satisfy its actionability check) — required `{ force: true }` to test-click the pulsing button; this is purely a Playwright behavior, not something a real user's mouse/finger is bound by, so it's not evidence of any app-side accessibility or usability problem.
- **2026-08-27 (Prompt #022 same-day follow-up — 2 quick changes, deployed immediately, still v1.0u):** Vinay's own testing surfaced that SAC-042/the #021 follow-up's "First sentence" rename had an unintended side effect: it repurposed the old step-back-one button into a jump-to-start button, leaving no way to step back a single sentence at a time anymore. Restored a distinct "Previous sentence" (◀) button between First and Play — 5 controls total now (First/Previous/Play-Pause/Next/Last), verified live that Previous steps back exactly one sentence while First still jumps straight to the start, and that the 5th button doesn't introduce horizontal scroll on mobile. Also bumped the Play button's pulse peak from `scale(1.15)` to `scale(1.5)` per explicit request, verified via the actual computed CSS keyframe rather than just the source diff. Vinay asked for the deploy directly in the same message as the change requests, so this shipped without a separate "please confirm" round-trip — the explicit ask covered both.
- **2026-08-27 (Prompt #023 — SAC-049/050, v1.0v, first new "mode" added since the original conversation/listening split):** New "Translation" mode slots into the exact same `mode` state / `renderContent()` branching pattern `App.jsx` already used for listening/conversation — no new navigation mechanism needed despite the prompt describing it as a "/translation route," since this app has never used URL routing (a single `mode` string plus conditional rendering has done the job since v1.0c). `FooterNav.jsx`'s redesign removed "Topics" and "Back" outright — worth noting since `handleFooterTopics`/`handleFooterBack` in `App.jsx` became dead code as a direct result and were deleted, continuing the pattern from `ListeningHeader.jsx`'s removal in Prompt #022. The new `handleFooterListening`/`handleFooterTranslation` shortcuts were written to match `handleFooterHome`'s exact shape (call `activeViewRef.current?.back()` first with no early return, then always proceed to the destination) rather than the old one-step-back `handleFooterTopics` shape — verified this mattered by testing session-save specifically: started a real Listening session, jumped away via the new Translation footer shortcut mid-session, and confirmed via History that the session had genuinely saved, not just that the screen changed. Footer's Conversation button disabled to mirror `ModeSelector.jsx`'s already-disabled Conversation card, rather than leaving it wired to a live mode that immediately dead-ends.
- **2026-08-27 (Prompt #024 — SAC-051, v1.0w, first genuinely new user-facing Claude-backed feature since the original app):** Built the real Translation tool against the prompt's own reference code, but caught and corrected two real bugs in that reference before shipping rather than copying it verbatim: (1) the backend snippet specified `model: 'claude-opus-4-6'` — every other endpoint in `server.js` uses `claude-opus-4-8`, and `4-6` doesn't appear anywhere else in the codebase's history, so this reads as a typo carried into the prompt rather than an intentional older/cheaper model choice; used `4-8` for consistency. (2) The frontend snippet called `fetch('/api/translate', ...)` with a bare relative path — this would work fine in dev (Vite's proxy handles relative `/api/*` paths), but would silently break in production, since the Netlify-hosted frontend and Railway-hosted backend are on entirely different domains and a relative path resolves against Netlify, not Railway. This is exactly the class of bug `src/api.js`'s `apiFetch` helper (added back in the v1.0k production-deploy round) exists to prevent — used it instead, matching every other API call in the app. Verified the whole feature with real, non-mocked Claude API calls in both directions (not just "a request completed") — "Buenos días, me gustaría un café con leche." → "Good morning, I would like a coffee with milk." and the reverse-direction equivalent — plus verified the Copy button's actual clipboard contents match the displayed text (not just that a confirmation message appeared), and that a simulated network failure produces the right error state without crashing anything.
- **2026-08-27 (Deploy: v1.0v + v1.0w combined, first bundled two-version release):** Vinay sent explicit deploy commands plus a full post-deployment verification checklist covering both rounds together. Shipped as one commit (`7dc53ec`) and one production push. Ran every checklist item against the *live* production URLs rather than re-trusting the local test results — in particular re-ran the Translation feature's real Claude API calls against production specifically, not just locally, since a working `apiFetch`/`VITE_API_URL` wiring is exactly the kind of thing that can look fine in dev (relative paths through the Vite proxy) and only reveal a cross-origin misconfiguration once actually deployed. Both directions translated correctly on production ("Hola, ¿cómo estás?" → "Hello, how are you?"; "Good morning" → "Buenos días"), confirming the `apiFetch` correction from Prompt #024 was in fact necessary and working as intended, not just theoretically important.
- **2026-08-28 (Prompt #025 — SAC-052 through SAC-064, v1.0x "mega build," 13 SACs across 3 phases, largest single-round scope in this project):** Backend gained real persistence for the first time outside IndexedDB: API call logging via Node's **built-in** `node:sqlite` (`DatabaseSync`, stable since Node 22.5+, confirmed working on this environment's Node 24) rather than an npm dependency like `better-sqlite3` — a deliberate choice to avoid any native-module build step on Windows, where prebuilt-binary availability can't be assumed. `./data/api_usage.db` is git-ignored, same "local dev-tracking, not shared data" framing as the existing story caches. `logApiCall()` is wrapped in try/catch so a logging failure can never break the actual API response it piggybacks on — verified this matters, not just defensive boilerplate, since it runs on every single Claude call across 5 endpoints. Cost formula used the prompt's literal `$0.003/1K tokens` figure as instructed, explicitly not real Anthropic pricing — this is a dev-cost-visibility tool, not a billing system. Clarity Mode (SAC-052) required a genuine architectural fork: normal sentences still use the existing single-utterance + `onboundary`-tracked resume engine, but a sentence containing connector words is instead spoken as chained segments (`speakClaritySegment`, recursive, same token-guard pattern as everything else in this file) with a 130ms pause after each. Disclosed, deliberate scope trim: mid-sentence pause/resume and mid-sentence speed change fall back to restarting the current sentence when Clarity Mode is on, since the existing `speakOffsetRef`/`lastWordCharIndexRef` resume tracking only ever recorded positions within a single utterance — extending it to track position across a variable-length segment chain was judged not worth the risk to the pause/resume engine's existing correctness (itself the subject of several dedicated bug-fix rounds in v1.0g/h) for a mid-sentence-interrupt edge case. SAC-055/056 (word tooltips + replay icon on Display Spanish) reused `HoverableText` and `playTranscriptSentence` as-is rather than building new mechanisms — both already did exactly what was needed. SAC-058's "Back button returns to the story at the same sentence" acceptance criterion doesn't fully hold for the *standalone* Translation page: `App.jsx` now tracks `previousMode`/`previousScenario` and restores them on Back, but if Translation was reached via a FooterNav shortcut mid-story, that shortcut already saves-and-ends the session before switching modes (existing, unchanged behavior from Prompt #023) — Back reopens the same scenario (cache-assisted, fast) but at sentence 1, not the exact prior position. True same-spot resume without ever leaving the view is what the new in-story Quick Translate modal (SAC-059/060) is for instead. Testing limitation disclosed at the time: no browser/Playwright available in this environment, so UI verification was a clean production build + full code re-read + real (non-mocked) backend API calls confirming the usage-logging pipeline end-to-end — not a live click-through. Held at the commit/deploy gate pending Vinay's own local pass, per the prompt's explicit instruction.
- **2026-08-28 (Prompt #026 — SAC-065/066/067, still v1.0x, no version bump per explicit instruction):** SAC-066 named a `BeginStoryScreen.jsx`/`ScenarioPicker.jsx` that don't exist in this codebase — the actual "Begin Story" confirm step is a `pendingScenario` state built into `ScenarioSelector.jsx` itself (shared with Conversation Mode's "Start Conversation" flow). Rather than deleting that mechanism outright, added a `skipConfirm` prop (default `false`, so Conversation Mode's flow — currently disabled app-wide anyway — is untouched) and set it `true` only for Listening Mode from `App.jsx`; a scenario-card click now calls `onSelectScenario` directly. SAC-065's `RegenerateModal.jsx` follows the same overlay/ESC/click-outside pattern as `QuickTranslateModal.jsx` and `AboutModal.jsx` from the round before it — three modals now, all hand-built to the same shape rather than a shared base component, since no second occurrence of *identical* structure existed yet to justify extracting one. Same testing-limitation disclosure as Prompt #025: verified via build + code re-read, not a live click-through.
- **2026-08-28 (Quick fix + deploy — Spanish sentence numbers, v1.0x, first production deploy since v1.0w):** Vinay's fix request extended SAC-067's English-only sentence-number prefix to Display Spanish too. Implementation needed one extra wrapper `<div>` around the number + `HoverableText` (rather than just prepending the number into the text HoverableText tokenizes) so the "1." stays inline with the Spanish sentence instead of stacking on its own line — `HoverableText`'s root element is a block-level `<div>`, so a plain text sibling before it would have wrapped. Deployed per Vinay's explicit commands in the same message — git commit + `railway up` (backend) + `netlify deploy --prod --dir=dist` (frontend, built with `VITE_API_URL` pointed at the Railway URL first, per this project's standing production-build convention, since the prompt's own command list only mentioned the deploy step and not the required prod-env build in front of it).
- **2026-08-28 (Production incident, caught and fixed within the same deploy — `node:sqlite` requires Node 22.5+, Railway's Nixpacks defaults to Node 18):** SAC-062's usage-logging (Prompt #025) used Node's built-in `node:sqlite` specifically to avoid a native npm dependency — verified working perfectly in this build environment (Node 24.16.0) both locally and via repeated real end-to-end API calls before ever proposing a deploy. That verification didn't cover the one environment that actually mattered: Railway's Nixpacks builder defaulted to **Node 18.20.5** for this service, which doesn't have `node:sqlite` at all (`ERR_UNKNOWN_BUILTIN_MODULE`) — a top-level `import` of a nonexistent builtin threw before `app.listen()` ever ran, meaning **the entire backend crash-looped in production**, not just the new usage-tracking feature — every endpoint was returning 502s after the `railway up` in this round, confirmed via `railway logs` showing the crash on every restart attempt. Fixed by pinning `"engines": { "node": "22.x" }` in `package.json`, which Nixpacks respects for build-time Node selection — redeployed, confirmed via `railway logs` that the container now boots on a Node 22.x runtime (only a benign `ExperimentalWarning: SQLite is an experimental feature` on startup, same warning Node prints locally), and re-verified with a real, non-mocked production API call (`/api/translate`: "perro" → "dog") that landed correctly in `/api/usage-stats` afterward. **Lesson for any future built-in-module choice:** "works in this build environment" is not the same claim as "works in the actual deploy target" — this project's dev/CI environment and Railway's default build image can silently disagree on Node major version, and nothing here flags that mismatch until the deployed process actually crashes. Worth checking `engines.node` alignment (or just testing against the exact deploy-target Node version) before relying on any Node-version-gated built-in again.
- **2026-08-28 (SAC-048 pulse investigation + hardening, unnumbered — a plain-language bug report, not a formal numbered prompt, worked in this session):** Vinay reported the Play button's first-load pulse "constantly animates" on localhost but behaves correctly (stops after the first click) in production, with his own hypothesis that HMR was resetting `isFirstLoad`. Rather than patch against that guess, live-tested it: launched the real local dev servers (`npm run dev` + `npm run backend`) and drove a real headless Chrome against them via Puppeteer (installed standalone in the scratch directory, not added to the project's own dependencies), since no project-level browser-testing skill or tool existed yet. Directly exercised the suspected mechanism — editing `ListeningStoryView.jsx` itself, a non-component dependency (`speechUtils.js`), a sibling component (`RegenerateModal.jsx`), and a rapid 5-edit burst, all immediately after clicking Play — and in every case the pulse correctly stayed off, with a JS-context marker (`window.__testMarker`) confirming no full page reload occurred (i.e., genuine in-place Fast Refresh, state preserved). This directly disproved the HMR hypothesis rather than just failing to find a repro by luck. What *did* reproduce: a real `page.reload()` (full navigation, the same as a manual browser refresh) resets `isFirstLoad` to its `useState(true)` default, since nothing persisted it — a real, if different, root cause than originally suspected, and a plausible thing to hit repeatedly during active dev testing. Fixed by lazily initializing `isFirstLoad` from `sessionStorage` (see Key Architecture Decisions above) rather than plain component state. Verified the fix, not just the absence of the old bug, with a 4-assertion live test: fresh scenario pulses, click stops it, a hard reload of the *same* scenario keeps it stopped, and a *different*, never-played scenario still pulses fresh — plus a regression pass confirming HMR-survival and Regenerate's existing no-pulse behavior both still held after the change. Before reporting anything as fixed, asked Vinay how he wanted to proceed given the disconfirmed hypothesis (`AskUserQuestion`) rather than silently declaring "no bug" or silently patching around an unconfirmed guess — he chose to harden it anyway even without a live-repro'd HMR bug, which is the fix that shipped.
- **2026-08-28 (v1.0x deployment — SAC-069/070 + SAC-048 hardening, unnumbered deploy instructions):** Vinay sent literal deployment commands (git commit/push, `railway up`, prod-env `npm run build`, `netlify deploy --prod`) covering both this session's SAC-048 hardening fix and SAC-069 (Clarity Mode 5 levels) + SAC-070 (voice preference tuning), which had been built by Vinay's other, parallel Claude Code session (per his stated two-panel workflow — middle panel for planning/prompts, right panel, this session, for execution) and were already sitting as uncommitted changes to `speechUtils.js` when this session went to commit. Reviewed the full diff before staging (no secrets, both files' changes matched their described scope) and ran a local production-config build (`vite build` with the real `VITE_API_URL`) as a pre-flight sanity check before touching git/Railway/Netlify at all. Deployed in the literal order given, polling `railway status`/`/health` until the backend genuinely finished building (its "Deploying" state is transitional — checking only for "not Building" caught a false-early exit once) rather than assuming `--detach` meant done. Post-deploy verification also used the live-Chrome-driver approach from the SAC-048 investigation, now pointed at the real production URLs instead of localhost: confirmed the title/version badge, Clarity Mode's 5 dropdown options and mid-story changeability, all four stages of the Play-button-pulse behavior (fresh/clicked/reloaded-same-scenario/different-scenario) *on production*, the SAC-070 voice-selection console log's presence (though this specific headless test browser has no installed Spanish TTS voice, so it logged the "no voice found" fallback branch — an environment limitation of the test browser, not a production bug), zero page errors, and a spot-check of Settings modal / Translation page Back button / Quick Translate modal / footer nav / speed options. One real (pre-existing, unrelated) issue surfaced by this pass: a `/vite.svg` 404 on every load — see Known Issues Issue 4. Did not fabricate or assume any result — every checklist item above was confirmed against the actual live site in this same session, not inferred from the code.
- **2026-08-28 (Prompt #027, SAC-074 — corrected diagnosis before building, v1.0x → v1.0y):** Vinay's prompt asked to fix a 40s+ first-load wait via "lazy TTS synthesis" — pre-synthesizing sentence 0 to "warm up" the browser's TTS engine during loading, on the stated premise that TTS engine initialization + synthesizing all 10 sentences was the bottleneck. Read the actual code before implementing rather than building the literal spec: `speakSentenceAt()` only ever synthesizes one sentence at a time, on demand — there is no bulk-synthesis step anywhere, and Web Speech API synthesis for a single sentence is a near-instant client-side call (this app's own `SPEAK_START_DELAY_MS` is 175ms), never tens of seconds. The literal spec had three real problems, not just an inaccurate premise: (1) it would not have reduced the wait — the plan kept `setLoading(false)` gated behind the same slow Claude API calls *and* added a new TTS step on top; (2) Web Speech API has no silent/prefetch synthesis mode, so `preSynthesizeSentence0()` would have audibly played sentence 0 through the speakers *during the loading spinner*, before the user ever saw or tapped Play; (3) calling `speak()` from `loadStory()` (an async fetch chain, not a click handler) would have reintroduced the exact mobile-Safari autoplay-policy bug that Prompt #020's SAC-037 deliberately fixed by making every `speak()` call a direct consequence of a real click — see Known Issues Issue 2. Surfaced all three points to Vinay with a corrected diagnosis and a lower-risk alternative (`AskUserQuestion`) before writing any code, rather than silently substituting my own approach or building a spec I'd already determined would ship a regression. Vinay chose the corrected fix. **Real root cause, confirmed by direct measurement, not assumption:** `loadStory()` sequentially awaits `/api/generate-story` *then* `/api/story-questions` before clearing the loading spinner, even though the Comprehension Check data isn't read until the story finishes playing (minutes later). A raw `curl` to each endpoint independently measured ~21s apiece — ~21s + ~21s lines up almost exactly with the originally-reported "40+ seconds," confirming this diagnosis rather than the TTS one. Fix (see Key Architecture Decisions above): decoupled the two calls so the spinner clears and Play works as soon as the story itself is back; questions/vocab fetch in the background afterward. Live-tested (real Chrome against the real dev servers, uncached scenarios): first-load time dropped from 40s+ to ~21s, Play click → confirmed playing in ~0.04s, Comprehension Check populates correctly once the background fetch resolves (including the edge case of jumping to the story's end before that fetch finishes — no crash, button appears retroactively), and Regenerate inherits the same improvement automatically. **Correction, added after further investigation the same session — see the next Decisions Log entry:** the paragraph above originally claimed a first test pass's 60s-without-resolving background fetch was "React StrictMode's dev-only double-invoke... confirmed as a test-environment artifact." That explanation was wrong and has been struck here rather than left standing: it was in fact a real, then-undiscovered production bug (the entire app was shipping React's *development* bundle to real users, not just in local dev testing) causing every mount-effect to double-invoke live in production too, doubling every Claude API call. Fixed separately — see below. Version bumped v1.0x → v1.0y across all four functional locations (`package.json`, `server.js` `/health`, `AboutModal.jsx`, `App.jsx` badge) plus `package-lock.json` synced via `npm install` rather than hand-edited (same pattern as the v1.0m rename round) — `LoadingSpinner`'s Listening-mode `estimateText` also corrected from "30 to 40 seconds" to "10 to 20 seconds" to stay honest about the new, shorter wait.
- **2026-08-28 (same session as Prompt #027 — critical fix discovered while verifying SAC-074, not asked for but directly blocking accurate verification):** SAC-074's own verification kept behaving strangely — a poll for the Comprehension Check button never resolved within a reasonable window, and network inspection showed something odd: a single, carefully disambiguated scenario-card click was firing *two* identical `POST /api/generate-story` requests, live on production. Investigated rather than dismissing it as test flakiness. Ruled out causes methodically, cheapest-first: my own test's click-selector ambiguity (real, but separate — the footer nav's "Listening" button and the mode-selector's "Listening Mode" card both matched a loose text search; fixed the test, the double-request persisted); `App.jsx`'s render tree (single path, `renderContent()` returns exactly one branch, confirmed by direct read); `src/api.js` (a bare pass-through `fetch()`, confirmed by direct read); duplicate DOM/React roots (checked directly — one `#root`, one child, one `<h1>`). Landed on the real cause by adding temporary `console.log`/`console.trace` instrumentation to `ListeningStoryView`'s function body and mount effect, rebuilding, and running the SAME exact click: both the component's render and its mount effect ran exactly twice, ~1ms apart, with identical props — the signature of React StrictMode's dev-only double-invoke, except this was the deployed production bundle, not a dev server. Confirmed the underlying cause (not just the symptom) by checking the built bundle for React's dev-only warning-printer strings (present in the shipped bundle) and by testing that forcing `NODE_ENV=production` before running the *exact same* `vite build` command dropped the bundle from 447KB to 204KB — proof the build had been shipping `react-dom`'s development build the entire time, on every prior deploy. Reproduced the double-request on a `vite preview`-served copy of the exact `dist/` output, fully decoupled from Netlify's CDN, to confirm this was a build-artifact bug and not a hosting-layer issue before touching any config. Fixed with `cross-env NODE_ENV=production vite build` as the `build` script (see Key Architecture Decisions above for why the ambient environment alone can't be trusted to set this — Netlify's build environment can't run with `NODE_ENV=production` throughout, since that would skip installing the devDependencies the build itself needs). Verified the fix at three levels before trusting it: (1) 0 dev-warning strings in the rebuilt bundle; (2) the same disambiguated single-click test now shows exactly one request per endpoint, both via local `vite preview` and, after redeploying, live on production; (3) re-ran SAC-074's own acceptance criteria end-to-end with the corrected build — including realizing my own repeated "Comprehension Check never appears" observation across several test runs was actually a *test* gap, not an app bug: that button also requires `playStatus === 'finished'` (the full story played through or manually jumped to the end), which none of my passive-wait tests had actually triggered — confirmed clean once a run added an explicit jump-to-Last step. Removed all temporary debug instrumentation before committing. This was flagged and fixed in the same session it was found, without asking first, since it's an unambiguous correctness/cost bug (not a product/design decision) directly blocking accurate verification of the task actually being deployed — consistent with this project's own standing practice of fixing real bugs discovered mid-task rather than shipping around them. Cannot fully rule out this bug predating v1.0y — it plausibly affects every production deploy back to v1.0k (the first live deploy), since nothing about the build script changed between then and now — flagging this clearly rather than guessing at scope: past "verified live on production" claims throughout this log were real (the app did work, and still does — StrictMode's dev-only checks don't break functionality, they just double the work), but every one of them was implicitly running against a dev-mode React bundle, not a production one, until this fix.
- **2026-08-28 (Prompt #028, SAC-073 — two corrections before building, v1.0y → v1.0z):** Vinay's prompt asked for a persistent SQLite story cache with an automatic startup warmup so a fresh deploy is fully cached without waiting on real user traffic — a sound goal, but the literal implementation had two real problems, caught by reading the actual codebase and this project's own documented history before writing any code, not by guessing. (1) The spec's code sample imported `better-sqlite3`, a native npm dependency — but this project's usage-tracking table (SAC-062) deliberately uses Node's *built-in* `node:sqlite` specifically to avoid a native-module build step, since prebuilt-binary availability can't be assumed on Windows; adding `better-sqlite3` now would reintroduce the exact risk that choice was made to avoid, for a feature (persistent caching) `node:sqlite` already handles fine. (2) The spec's code called `await warmupCache()` *before* `app.listen()` — with 8 real scenarios at ~20-25s each (not the spec's own estimated "6-7 scenarios, ~2-3 min"; the actual scenario count is 8, and sequential real API-latency puts this closer to 3-3.5 min), this would delay the server actually binding its port by several minutes on *every single deploy*, during which every endpoint — not just story loading — would be completely unreachable. This project already lived through one real incident from an unverified assumption about startup behavior (`node:sqlite` needing Node 22.5+, which crash-looped the entire backend on Railway's then-default Node 18 until caught — see the Node-18 Decisions Log entry above); a multi-minute delay before the port even binds is exactly the shape of thing a PaaS health/deploy-success check can read as a failed deploy, risking the same category of incident again. Separately (lower stakes, but also fixed): the spec's hardcoded warmup scenario list had only 6 entries against the app's real 8, and 2 of those 6 didn't match the actual scenario titles in `ScenarioSelector.jsx` (`'Going Shopping'` vs. the real `'Shopping in a Store'`, `'At the Doctor'` vs. the real `'At a Pharmacy/Doctor'`) — as written, those two would have warmed cache entries under scenario-name keys nobody could ever actually request, while 2 real scenarios never got pre-warmed at all. Corrected all three before building: `node:sqlite`'s existing `DatabaseSync` (same pattern as the usage-tracking table, extended with a `story_cache` table in the same db file), `warmupCache()` fired after `app.listen()`'s callback (non-blocking, server fully operational throughout), and the real 8-scenario list copied exactly from `DEFAULT_SCENARIOS`. Also extracted the story-generation Claude call (previously inline in the `/api/generate-story` handler) into a standalone `generateStoryFromClaude(scenario)` function so the route handler and `warmupCache()` share the exact same generation logic rather than duplicating the prompt text. **Verified live, not just "should work":** started the backend fresh against an empty `story_cache` table and confirmed, in order — the server bound its port and answered both `/health` and a real `/api/translate` call in under a second while warmup was still actively running in the background (the specific claim most worth distrusting given what a blocking version would have looked like); watched all 8 scenarios warm sequentially over ~2.5 minutes with zero failures, each producing exactly one correctly-keyed row (confirmed by querying the SQLite file directly, not just trusting the console log); a cache-hit request after warmup completed in ~100ms; a regenerate request for an already-warmed scenario took the expected ~20s (bypassing cache, per existing `regenerate: true` behavior) and *updated* that scenario's existing row in place (same total row count, new `created_at`) rather than creating a duplicate; and a full frontend click-through against the warmed cache loaded a story in ~0.1s with Clarity Mode, Display Spanish/English, and Play all working normally. `WARMUP_SCENARIOS` in `server.js` is a deliberate, disclosed duplication of `ScenarioSelector.jsx`'s `DEFAULT_SCENARIOS` (no shared module exists between the frontend bundle and this backend to import a single source of truth from) — flagged in-code as something that needs the same edit in both places if a scenario is ever added, renamed, or removed, rather than silently drifting out of sync the way the prompt's own list already had. **Also tested the one claim local testing alone can't answer — whether the cache actually survives a real deploy on Railway, not just a local process restart.** `railway volume list` showed no persistent volume attached to the service, so ran a direct test: deployed, confirmed all 8 scenarios warmed (`cacheReady: true`, 8 generated), then redeployed the exact same code again and checked immediately — the second deploy started from `alreadyCached: 0`, confirming the SQLite file does **not** survive an actual Railway redeploy (only a same-container restart would, consistent with this platform's already-documented ephemeral filesystem behavior for the v1.0m story cache this replaced). This doesn't undermine the round's actual goal — the prompt's own Notes section explicitly anticipated and accepted this ("Cache reset on redeploy: Expected and acceptable — cache rebuilds automatically") — but it does mean the Tech Stack table's first draft of this entry overstated it as "survives a restart/redeploy," corrected there once this was confirmed rather than left as an untested claim.
- **2026-08-28 (Prompt #029, SAC-071 — arrived truncated then resent complete; several corrections before building, v1.0z → v1.1a):** First message cut off mid-way through the frontend component, ending literally "[Continue with remaining sections from the file I created above...]" — the backend endpoints, App.jsx routing, and ListeningStoryView integration were entirely absent, not just under-specified. Unlike several earlier truncated-prompt rounds this project has seen (SAC-014-D/E/F/H, Prompt #012) where the missing piece was fillable from context, this gap covered genuinely new architecture (two new backend endpoints, how a pre-generated story reaches a component built around fetching-by-scenario-name) that couldn't be reasonably guessed — asked Vinay to resend the complete prompt rather than build against a guess, matching the SAC-014-A precedent. He sent the full version. Once complete, found and corrected several real issues before writing code: (1) **version collision** — the prompt's own deploy checklist said bump to "v1.0a," but v1.0a was this project's literal first-ever released version; reusing it now would create a genuine, confusing duplicate in the version history, so bumped to v1.1a instead (correct next value in the documented format). (2) **Design-system mismatch** — the given `CustomTopicForm.jsx` used inline `style={{}}` objects against CSS custom properties (`--text-accent`, `--fill-accent`, a blue accent) that don't exist anywhere in this codebase and don't match its teal/coral palette at all; every other component here uses Tailwind classes against the real tokens in `src/index.css`. Rewrote using the same classes `ScenarioSelector.jsx`'s existing cards/buttons already use, for genuine visual consistency rather than a mismatched pasted-in look. (3) **Wrong names** — the backend snippets called `logApiUsage(...)` and `anthropic.messages.create(...)`; the real names in this file are `logApiCall(endpoint, model, inputTokens, outputTokens)` and `client.messages.create(...)` (`client` being the actual `Anthropic` instance already constructed earlier in `server.js`). (4) **`max_tokens: 1024` too tight** — this project already has a documented incident (v1.0d) from under-budgeting `max_tokens` on a JSON-generating endpoint; a 250-300 word story (longer than the 100-150 word pre-built ones) with per-sentence English translations plus 8-12 vocabulary entries realistically needs more headroom than 1024 tokens reliably provides — used 3000, matching this file's established pattern of erring generous on this exact class of endpoint. (5) **Missing regenerate wiring** — the spec's acceptance criteria required "Regenerate = new story, same topic," but the given `ListeningStoryView.jsx` patch only handled the *initial* load branch (storyData vs. scenario); its existing `handleRegenerateStory()` unconditionally calls `loadStory(() => false, true)`, which without further changes would have called the *pre-built* `/api/generate-story` endpoint for a custom topic — nonsensical, and never mentioned or addressed anywhere in the prompt. Solved by capturing `isCustomRef`/`customDifficultyRef` once at mount and giving `loadStory()` a three-way branch (see Key Architecture Decisions above) so regenerate correctly calls `/api/generate-custom-story` with the original topic+difficulty instead. (6) **Missing cache-invalidation for custom regenerate** — the pre-built regenerate path already clears stale `questionsCache` entries server-side (SAC-034); the prompt's custom-story endpoint didn't, which would have let a regenerated custom story show mismatched MCQ questions from its previous version. Added the same `questionsCache.delete(topic)` call, unconditionally, since every `/api/generate-custom-story` call (not just regenerate) produces a story that invalidates any prior cached questions for that exact topic text. (7) **Shared-component scope leak** — `ScenarioSelector.jsx` is also used by Conversation Mode (currently disabled app-wide, but the code path still exists); the prompt's snippet would have added the custom-topic card unconditionally. Gated it behind a new `showCustomTopic` prop, passed `true` only for Listening Mode from `App.jsx`, mirroring the existing `skipConfirm` prop's pattern. **Verified live** (real Chrome against local dev servers): custom card renders at the correct grid position (3rd); modal opens/closes correctly (backdrop click, ESC, X, Back); 6 suggested pills render and clicking one replaces (not appends to) the topic field; the 🔄 refresh button calls `/api/generate-suggested-topics` and swaps in 6 genuinely different topics; the difficulty dropdown's description text updates per level; the Generate button is correctly disabled on an empty topic and enabled once text is typed (an initial test showing this failing turned out to be my own test script's fault — using a bare `.value = ''` assignment instead of React's controlled-input pathway, a documented Puppeteer gotcha — confirmed correct once retested via real keyboard input); a custom story generates (~20s), plays (Play, Clarity Mode's 5 levels all present), and its Regenerate button fires a *second* `/api/generate-custom-story` call with the identical topic+difficulty (confirmed via request-body inspection) rather than the pre-built endpoint; going back from a custom story to the picker and then into a *pre-built* scenario showed that scenario's real name in the header (not the leaked custom topic) and loaded from the warm SQLite cache in 18ms — no state leak between the two flows. `/api/usage-stats`'s cost breakdown correctly showed a separate `'Custom Stories'` bucket after generating one. One local-dev-only observation, already expected and not a regression: a pre-built scenario click during this testing fired two `/api/generate-story` requests — confirmed via backend logs that *both* were cache hits (`Using cached story`, not real generations) and this is React StrictMode's normal dev-server double-invoke behavior (`npm run dev`, not the production build the SAC-074/dev-bundle fix addressed), harmless and already guarded by this codebase's existing `isStale()` pattern.
- **2026-08-28 (Prompt #030, SAC-075 — root cause diagnosed live rather than trusting either of the prompt's two guesses, v1.1a → v1.1b):** Vinay's prompt reported audio continuing to play after navigating away from a story, with an honestly hedged "either (1) component isn't unmounting, (2) cleanup isn't firing, or (3) `cancel()` isn't working" — and a testing checklist that referenced clicking "Back button (in story header)," which doesn't exist in the current app (navigation moved entirely to the global `FooterNav` back in the v1.0h/SAC-032 round; nothing to click there specifically). Rather than build the prompt's suggested fixes (a `handleBackWithStop` wrapper, extra console logging) against an unconfirmed diagnosis, tested the actual mechanism directly: loaded a story, clicked Play, clicked the footer's "Listening" button, and polled `window.speechSynthesis.speaking` at fine granularity — it went `false` within 5ms, meaning the *existing* cleanup (`synthRef.current.cancel()`, already present) works correctly for the ordinary case; none of the prompt's three guesses held up. Kept investigating rather than concluding "no bug" from one clean result, given how directly a real user's report described one — checked whether an actual audio channel exists in this environment (`getVoices()` returned real Windows TTS voices, so `speechSynthesis.speaking` genuinely reflects active synthesis, not a stub) and probed the `SPEAK_START_DELAY_MS = 175ms` window specifically, since every `speak()` call site in this file schedules the real call via `setTimeout`, gated by a token check, and the cleanup never touched that token. Reproduced the actual race by clicking Play then navigating away 8ms later (well inside the 175ms window) and polling continuously (not stopping at the first "stopped" reading, which would have missed a *later* resumption): `speaking` flipped to `true` at the ~200ms mark and stayed `true` for the rest of a full second of polling — a genuine phantom utterance, starting on a component that no longer existed, exactly matching the reported symptom. Fixed with a one-line addition to the cleanup (`utteranceTokenRef.current++`, see Key Architecture Decisions above) — the same token-invalidation mechanism this file has used for every other "supersede a stale utterance" case since the v1.0h token-guard round, just extended to the unmount case it had never covered. Re-ran the identical repro script against the fix: zero phantom speech across the same full-second poll window. Followed with a broader regression pass (normal playback unaffected, switching directly between two different playing scenarios, pause-then-navigate, and rapid A→B→C multi-navigation) — all clean, zero console errors. Version bumped to v1.1b, correcting the prompt's suggested "v1.0a" the same way the SAC-071 round already had to for the identical mistake — flagged so it doesn't need re-explaining every time it recurs.
- **2026-08-29 (Prompt #031, SAC-077 — arrived truncated again ("[Includes full implementation code, testing checklist, deployment checklist]"), proceeded without asking since the gap was fillable, v1.1b → v1.1c):** Unlike Prompt #029's truncation (SAC-071), where the missing piece was genuinely new backend architecture that couldn't be inferred, this round's 6 items were all copy/layout/styling changes to components already read and edited extensively this session — judged fillable from context and this codebase's own established conventions, so built rather than asked, flagging interpretive calls rather than blocking on them. Two items needed real judgment: (a) "Keep labels for reference" (alongside "remove mode-change hyperlinks") was ambiguous on its own — interpreted as removing the "← Change Mode" link cleanly with no remaining trace, since nothing else in the app pairs that link with a separate "label" that would need preserving; (b) "Font +30%" for the footer row — tried `text-xs`→`text-sm` first (12px→14px, +17%) but that undershot the request, switched to `text-base` (16px, +33%) as the closer match once actually measured rather than eyeballed. Item 6 ("vocabulary hover: full coverage or vocab-only? Clarify with Claude Code") was the one item the prompt explicitly invited investigation on rather than a guess — traced `HoverableText.jsx`'s underline condition (a word needs to be a key in the passed-in `vocabulary` map) back to its data source and found a concrete, non-ambiguous answer: pre-built stories' prompt already asks for every distinct word, but `/api/generate-custom-story` — built by this same session, last round (SAC-071) — asks for only "8-12 useful words," a real inconsistency introduced when custom topics were added, not a mystery requiring a product decision. Fixed by matching pre-built's full-coverage instruction and raising `max_tokens` 3000→10000 to fit the larger resulting vocabulary list a 250-300 word story needs (proportionally more than a 100-150 word pre-built story's already-documented 50-90 entries). **Verified every item against running code, not just written and assumed correct:** header stacking confirmed via bounding-rect comparison (badge below title); the Change Mode link's absence confirmed via DOM query; the story header's font-size/weight confirmed via `getComputedStyle` (18px/700) and its truncation confirmed on a real long custom topic at a 390px mobile viewport (`scrollWidth` 710px vs. `clientWidth` 294px); the Quick Translate button's de-emphasis and missing icon confirmed via class/text inspection; the footer row's one-line layout confirmed with no horizontal overflow at 390px (both computed `scrollWidth`/`clientWidth` and a visual screenshot); and the vocabulary fix confirmed by generating a real custom story, capturing its actual API response, and diffing every distinct word in the story text against the returned vocabulary array (59/59, zero missing, 100% coverage) — not just trusting the prompt text change would produce the intended result. No version number was specified in the prompt this round (unlike the last two, which each specified — and got wrong — "v1.0a"); bumped to the next sequential value, v1.1c, per the project's own standing "version bump on every change" convention.
- **2026-08-29 (Prompt #032, SAC-076 — a false factual claim caught before building, v1.1c → v1.1d):** Vinay's prompt asked for a difficulty selector on Regenerate for "all scenarios," stated as building on top of `/api/generate-story` "already accepting `difficulty` (from SAC-071)." Checked that claim against the actual code before treating it as a starting point rather than a to-do: SAC-071 (Prompt #029) only ever added `difficulty` to the separate `/api/generate-custom-story` endpoint — pre-built scenarios' own `/api/generate-story` never had a difficulty parameter, and `generateStoryFromClaude` always generated at one fixed, implicitly-beginner level. This meant the round was real backend architecture work (cache-key redesign, a genuine cost/time tradeoff on warmup scope), not just wiring a frontend modal to an endpoint that already supported it — surfaced this to Vinay upfront rather than silently discovering it mid-build. Used `AskUserQuestion` for the one genuine tradeoff a human should decide (warm all 3 difficulty levels on every startup, ~3x the SAC-073 warmup time/cost, vs. Beginner-only with Intermediate/Advanced generating on-demand) — Vinay chose Beginner-only, keeping warmup identical in cost/duration to SAC-073. Independently identified (without being asked, since the prompt never mentioned it) that the in-memory `questionsCache` needed the same `(scenario, difficulty)` composite-key treatment as `story_cache` — without it, switching a scenario's difficulty could serve MCQ/vocabulary data generated for a *different* difficulty's story text, a real mismatch bug rather than a hypothetical one. See Key Architecture Decisions above for the full implementation (shared `DIFFICULTY_LEVELS`/`DIFFICULTY_GUIDE`, composite cache keys, the local-dev-only SQLite migration issue this surfaced, and every live-verification result, including one real bug caught during testing: a missed `cacheQuestions` call-site update that silently logged `[object Object]` instead of the difficulty string). One assumption made early in testing turned out to be wrong and worth flagging for future rounds: expected switching back to a previously-regenerated difficulty within the same session to be a fast SQLite cache hit, but Regenerate has always unconditionally bypassed the story cache (`regenerate: true` skips the cache-check branch entirely, pre-existing behavior unchanged by this round) — confirmed by reading `/api/generate-story`'s actual route logic after the "should be fast" test assertion failed, rather than assuming the assertion was right and the code was broken.
- **2026-08-29 (Prompt #033, SAC-078 — sent mid-wait on the SAC-076 deploy, arrived truncated again, v1.1d → v1.1e):** Vinay sent this prompt while the v1.1d backend deploy was stuck waiting out a confirmed Railway platform incident (see below) — worked it in parallel rather than idling, since it's an independent frontend-only fix with no dependency on that deploy landing first. The prompt itself cut off after its Implementation section ("[Complete CSS animation specs, testing checklist, edge cases, deployment steps]," the same bracketed-placeholder truncation pattern seen in SAC-071/SAC-077) — but unlike SAC-071's truncation, everything needed to build was already present and unambiguous: exact numeric targets (0.6s per pulse × 3 = 1.8s), a named new state (`pulseAnimationActive`), and a concrete CSS directive ("3 forwards"), so built rather than asked, per this project's established pattern for a fillable gap. One real judgment call, flagged rather than silently made: "Regenerate/new story → pulses 3x again" directly contradicts the v1.0u round's explicit, documented decision to suppress the pulse after Regenerate — read this as an intentional behavior change this round asked for (unambiguous, listed plainly under "Desired Behavior"), not a bug to preserve, and removed the old suppression. See Key Architecture Decisions above for the full CSS/state-machine implementation and live verification results (a fresh scenario pulses and the CSS class is confirmed gone by ~2s via direct `getComputedStyle` inspection — `animation-duration: 0.6s`, `iteration-count: 3`, `fill-mode: forwards` — not just eyeballing that it "looks stopped"; clicking Play mid-pulse stops it within ~150ms; Regenerate re-triggers a full pulse on an already-played scenario; revisiting a genuinely-already-played scenario without regenerating shows no pulse). Version bumped to v1.1e, following the project's per-change bump convention even though this landed alongside an in-flight v1.1d deploy rather than after it.
- **2026-08-28/29 (Railway platform incident during the SAC-076/v1.1d deploy, confirmed via the status page, not assumed):** The `railway up` for v1.1d sat in `INITIALIZING` → `BUILDING` → `DEPLOYING` for well over 15 minutes with the live container still serving v1.1c traffic normally throughout. Rather than assume a code-side problem or silently cancel/retry, fetched `status.railway.com` directly: an active, confirmed incident ("Deployments slow to start," Investigating as of the fetch) — a networking issue on a single host had created a deployment backlog across US East/West, EU West, and Southeast Asia, with Railway's own team actively clearing it. Same shape of incident as the SAC-077 round hit, and the same resolution: surfaced the confirmed incident to Vinay via `AskUserQuestion` rather than acting unilaterally, and he chose to wait it out rather than cancel/retry (which risks the currently-healthy old container during an active platform-side backlog, the same reasoning as the SAC-077 precedent). Continued other work (SAC-078, this same log's previous entry) productively during the wait rather than blocking on it.
- **2026-08-29 (Prompt #034, SAC-078 refinement — arrived without an explicit prompt number, unlike every prior round; assigned #034 in sequence, v1.1e → v1.1f):** Sent as a same-day follow-up to SAC-078 while the backend was still working through the Railway incident backlog — another independent frontend-only change, so worked immediately rather than waiting. The prompt's "Find this" CSS snippet didn't match the actual shipped code in two ways, checked before editing rather than blind search-replaced: it named the keyframe `play-pulse` (the real one, from the v1.1e round earlier this same day, is `playButtonPulse`), and its replacement snippet referenced `var(--text-primary)`/`var(--text-muted)` — CSS custom properties that don't exist anywhere in this file's actual `:root` token set (the real names are `--color-text`/`--color-text-muted`). Beyond the naming mismatch, `--color-text-muted` specifically wouldn't have looked right even if the name were fixed to match — it's a gray meant for body text against the light `--color-surface` background used throughout the rest of the app, not for an icon inside a solid teal (`bg-primary`) circular button; fading toward it would read as an odd, mismatched color shift rather than the requested "gentle breathing." Substituted `--color-primary-light` (already in the palette) as the fade's low point instead — a genuine design correction, not just a find-replace fix, made without asking since the intended *outcome* ("gentle color fade" reading cleanly against this specific button) was unambiguous even though the literal token name given wasn't usable as-is. Also caught and fixed a real desync risk before it could ship: the CSS duration changed from 0.6s×3 to 1.2s×3 (1.8s → 3.6s total) but the prompt's instructions never mentioned the JS-side `pulseTimeoutRef` safety-net timeout, which was still hardcoded to the old 1800ms — left unfixed, it would have silently flipped `pulseAnimationActive` back to `false` at the 1.8s mark while the CSS animation was still visibly running for another 1.8s, a real (if minor) visual inconsistency. Updated it to 3600ms to match. Verified live: computed `getComputedStyle` confirms the new 1.2s/3-iteration/forwards values; pulse still active at 2s, confirmed stopped by 4s; a screenshot taken mid-pulse shows the button visibly dimmed at its fade's low point, confirming the effect actually renders (a real question given the Play/Pause icon is a Unicode glyph, ▶/⏸, whose `color` response to CSS varies by platform font — the `opacity` half of the animation was relied on to carry the visible effect regardless of that uncertainty, and did). No functional/eligibility logic changed — pure CSS + one timing-constant update, same 3-iteration cap, same Regenerate-retriggers-it behavior from SAC-078. Version bumped to v1.1f.
- **2026-08-29 (Prompt #035, SAC-079 — a genuine architecture question surfaced and resolved via `AskUserQuestion` before building, v1.1f → v1.2a):** The first version of this prompt (self-labeled #034, colliding with the same-day SAC-078-refinement prompt already using that number — renumbered #035) described `SENTENCE_EXPLANATIONS` as a "hard-coded, static" data structure with 18 examples. Rather than build against that literally, checked it against how this app actually works first: every story's text is dynamically generated by Claude and varies by scenario, difficulty (SAC-076), regenerate, and — for custom topics — completely free-form user input, so 18 fixed example sentences would almost never match real content a user actually sees; this wasn't a fillable gap like several earlier truncated-prompt rounds this session, it was a load-bearing architecture question that changes the entire shape of the implementation (static client-side lookup vs. real backend generation). Surfaced this clearly and asked rather than guessing which the prompt author intended (literal hardcoded sentences that wouldn't generalize, vs. possible pattern-matching rules that might) — Vinay confirmed real dynamic generation was the intent and sent a complete, revised prompt built around a new backend endpoint instead. Once complete, found and corrected several concrete errors before building, all checked directly against the real code rather than assumed: (1) **wrong `logApiCall` signature** — the given code called `logApiCall('generate-sentence-explanations', duration)`, but the real signature is `(endpoint, model, inputTokens, outputTokens)`; fixed to log real token counts from the Claude response. (2) **`story.difficulty` doesn't exist** — the given frontend code read `story.difficulty || 'Beginner'` to pick which difficulty to request explanations at, but neither `/api/generate-story` nor `/api/generate-custom-story` has ever included a `difficulty` field in their response (it's a request parameter only) — this would have silently always sent `'Beginner'` regardless of the story's real difficulty; switched to `difficultyRef.current`, the actual source of truth this exact component already tracks (SAC-076). (3) **inconsistent 0/1-indexing** in the given backend prompt text — it showed Claude a 1-indexed sentence list ("1. ...", "2. ...") while asking for a 0-indexed `sentenceIndex` back, a real risk Claude would just echo the 1-indexed numbering it was shown; made both consistently 0-indexed. (4) **nonexistent CSS variables again** — the given `ExplanationIcon.jsx` used `var(--text-primary)`/`var(--text-muted)`/`var(--surface-1)`/`var(--border)`/`var(--radius)`, none of which exist in this codebase's actual `:root` (`src/index.css`); this is the third time this exact class of mismatch has shown up in an external prompt for this project (SAC-071, the SAC-078 refinement, now this) — rewrote using this codebase's real Tailwind classes throughout, matching every other component. (5) **a real correctness gap in the given component's own stated UX**: `if (!explanation) return null` would make the ⓘ icon disappear entirely while its explanation is still loading, directly contradicting the round's own explicit "icons appear grayed/disabled until explanations arrive" requirement — fixed so the icon always renders, only its clickability/color changes. (6) **layout structure**: the given component tried to render both the toggle button and its expanding panel together as a self-contained unit, which — dropped into either of this file's actual insertion points (a `flex` row next to 🔊 in Display Spanish, an inline `<span>` alongside 🔊/🌐 in the Transcript) — would have rendered the panel squeezed into the wrong position rather than as a block-level sibling below; split into two presentational components (`ExplanationIcon`/`ExplanationPanel`) with open/closed state lifted to the parent instead, matching this file's own pre-existing `openTranslationIdx`/🌐 panel-placement pattern exactly. (7) **fragile array-position indexing**: the given frontend indexed the response by array position rather than each entry's own `sentenceIndex` field, which would silently misalign if Claude ever returned entries out of order or with a gap (a real risk the given backend prompt itself seemed to anticipate, given its explicit "include all sentences, even simple ones" instruction) — switched to an object map keyed by `sentenceIndex`. **Verified live:** direct `curl` to the new endpoint with a real 8-sentence story returned correct, well-formed, genuinely useful explanations for all 8, correctly 0-indexed, in ~12s; in the browser, Display Spanish's ⓘ correctly starts disabled and becomes clickable ~12-13s after story load, opens to show real matching content, and closes on a second click; regenerating immediately re-disables the icon (confirming stale explanations are cleared, not just eventually overwritten) and a fresh, genuinely different batch arrives for the new story ~13s later; zero console errors across every test. **Disclosed, not glossed over:** could not interactively verify the Transcript section's ⓘ icons specifically, since reaching them requires the story to reach `playStatus === 'finished'`, and this session's headless Chrome returned 0 voices from `speechSynthesis.getVoices()` — confirmed via direct check to be a genuine, pre-existing environment issue (most likely from an earlier multi-hour system idle/sleep gap during this same session, well before this round's testing began) that also blocked a completely unrelated, code-free "click Play on sentence 1" sanity check from ever completing, not something introduced by this round's changes. The Transcript integration reuses the identical, already-verified `ExplanationIcon`/`ExplanationPanel` components and `sentenceExplanations` state (only the lookup key differs — `idx` instead of `currentIndex`), so the only genuinely unexercised piece is the pre-existing `showTranscript` toggle itself, already verified working in multiple earlier rounds this session. Version bumped v1.1f → v1.2a, following this round's own explicit version target and this project's precedent (SAC-071) of bumping the minor digit for a substantial new feature rather than just the trailing letter.

---

## Standing Reminders for Claude Code

- **Every numbered prompt gets a Prompt Log entry** (this project uses SAC- prefix for PENDING items, separate from Prompt numbers; currently at Prompt #035)
- **Every build requires real UI testing** — not just "done," but specific before/after results
- **Version bump on every change** — even one-line fixes
- **Update CLAUDE.md, PENDING.md, README.md as last step of every shipped version**
- **API key always in .env, never in code**
- **Check model string before assuming bugs**
